# Session Efficiency Suggestions — Ranked by Recurrence

> Maintained by `.claude/skills/session-close/SKILL.md` Step 4b. `Count` = number of sessions
> where this exact root cause recurred, i.e. how many times fixing it would have helped — not
> a bug tracker. Sorted by `Count` descending. Do not hand-edit `Count`; the skill owns it.

| Count | Slug | Suggestion | Category | First seen | Last seen | Example |
|---|---|---|---|---|---|---|
| 23 | reread-file-already-in-context | Edit against the copy of a file already loaded by the Step-1 read instead of issuing a second scoped `Read` of the same file to grab exact-string context. | token-efficiency | 2026-08-27 | 2026-09-03 | SWEEP-6 — `Read(CLAUDE.md)` issued to satisfy the Edit prior-read gate although the file was already resident in the system prompt (project_docs), and `Read(AGENTS.md)` similarly after intervening bash greps; SWEEP-5 — `Read(CLAUDE.md)` issued twice (once to satisfy the Edit prior-read gate with the file already resident in the system prompt, then again after intervening bash greps) before the CLAUDE.md edits; SWEEP-4 — `docs/plan/README.md` Read, Edited, then Read again at a different offset for the next Edit anchor, and `commit/SKILL.md` re-consulted after its Step-1 read; SWEEP-3 — `suggestions-sweep/stories.md` Read 3× for Edit anchors, and `Read(CLAUDE.md)` issued to satisfy the Edit prior-read gate when the file was already resident in the system prompt; SWEEP-2 — `scripts/dev/hooks/check_md_line_length.py` Read 3× in one session (twice back-to-back at the open as the `.sh`→Python pattern reference, then again for an Edit anchor); SWEEP-1 — `suggestions-sweep/stories.md` Read ~5× in one session (two back-to-back at the open, then again after each reflow pass) to re-fetch Edit anchors; RDO-17.7 — `scripts/dev/reflow_md.py` re-Read at offsets 40–80 and 165–176 for Edit anchors minutes after authoring it; ROLL-7 — `src/strategy/reentry_mixin.py` re-Read at offsets 55/125/230 to verify edits already made; MEAS-1 — `docs/plan/token-efficiency/measurement/tasks.md` re-Read at offset 7 right after a bash `cat` had already shown the same lines; MEAS-2 — `docs/plan/token-efficiency/README.md` full-read then re-Read at offset 28 for an Edit anchor; FIX-1 — `Read(CLAUDE.md)` issued to satisfy the Edit/Write prior-read gate when the file was already resident in the system prompt; FIX-4 — `Read(CLAUDE.md)` again for the same gate, plus `scripts/dev/graph_snippet.py` re-Read twice for Edit anchors right after authoring it |
| 9 | pytest-inlined-not-test-runner | Spawn the `test-runner` (Haiku) subagent for the post-edit test run instead of running `pytest` inline in the main session's context. | agent-routing | 2026-08-27 | 2026-09-03 | ROLL-6 EOD summary migration — ran `pytest tests/unit/` inline ~4× across the session, never spawned test-runner; ROLL-7 re-entry notice — full `pytest tests/unit/` run inline 2× plus ~6 focused inline runs alongside 3 test-runner spawns; MEAS-1 — targeted + full-suite `pytest` run inline 3× across the session, never spawned test-runner; FIX-4 — `pytest` run inline ~5× (targeted + full `tests/unit/`) across the session, test-runner never spawned |
| 7 | wide-grep-dump-then-page | Scope a discovery grep/sed to `-c` counts or a narrow line range before running it — a wide match over a 2000+-line file writes a huge tool-result file that then costs a 25K-cap Read to page into. | token-efficiency | 2026-08-27 | 2026-09-02 | round-2 token-opt #3 — a combined `grep -noE + sed` over DECISIONS.md produced a 99 KB result file; paging into it hit the 25K cap on page 1 of 180; FIX-2 — an unscoped `grep -rn` for three CLAUDE.md path spellings across the whole repo returned multi-thousand-char `docs/archive/**` prose lines (~2.5K tokens) where `--exclude-dir=docs/archive` plus `cut -c1-140` was wanted |
| 6 | sha-recorded-via-second-commit | For a docs-only phase whose `tasks.md` entry records its own commit SHA, land one commit with the SHA line as `<pending>` and backfill the real value as the first edit of the next doc-touch — a dedicated second commit that only swaps `<pending>`→SHA costs an extra full pre-commit stash/restore cycle for nothing. | commit-hygiene | 2026-08-28 | 2026-09-03 | SWEEP-3 — e325e86 landed the hook work with `<pending>`, then 1baae59 did nothing but swap in the SHA and tick the box; RDO-10 — a4431ec landed with `<pending>` placeholders, then b7d92df did nothing but replace them; RDO-17.7 — 610fd8f only swapped `<—>`→`526e431` and ticked the box; MEAS-2 — a0dfa38 (baseline) then 6377c08 doing nothing but ticking the box + recording a0dfa38 (conflicts with tasks.md's own "one commit plus a follow-up" convention — needs protocol reconciliation, not a per-session fix); FIX-1 — 41ac31e (work + As-built at `<pending>`) then 2f6be3b doing nothing but ticking the box and swapping in the SHA, following the MEAS-1/MEAS-2 precedent found in git log. **Count now ≥5 — this is the SWEEP-7 escalation threshold; it is a protocol conflict, not a per-session lapse, and should be escalated rather than re-logged.** |
| 5 | authored-md-prose-over-200-cap | When authoring or fully rewriting a hook-covered `.md` (root / `docs/plan/**` / `docs/bugs/**`), wrap prose to ≤200 chars as you write — or run the reflow helper right after each `Write`, before the first `Edit` — instead of Writing long single-line paragraphs and discovering 30+ over-cap lines only at hook-check time. | token-efficiency | 2026-08-29 | 2026-09-03 | SWEEP-1 — the added cluster prose in `suggestions-sweep/stories.md` tripped the `awk 'length>200'` check across three separate rewrite rounds before the pre-commit md hook would pass; RDO-17.6 — 11 files Written with 200–500-char prose lines; RDO-17.7 — the hand-authored RDO-17.7 / RDO-17.8 `tasks.md` lines at 240–251 chars (task lines can't be reflowed), caught only at `check_md_line_length` time and needing terse rewrites; doc-format-migration — DFM task lines drafted at 200–264 chars, wrapped by reflow, needed 2–3 terse-rewrite rounds; FIX-1 — three separate over-cap rounds in one session while rewriting CLAUDE.md/AGENTS.md: 4 table rows at 203–317 chars, then 1 at 234, then a 213-char epic-README row that **aborted the close commit** and cost a full pre-commit stash/restore + re-stage + re-commit cycle |
| 4 | graph-tools-unused-relied-on-grep | Use search_code(pattern)/get_code_snippet(qualified_name) from codebase-memory-mcp for src/scripts investigation before falling back to raw bash grep/sed — a whole session can otherwise pass with zero graph-tool calls despite touching many src/ files. | token-efficiency | 2026-09-01 | 2026-09-03 | SWEEP-4 — `check_checkbox_consistency.py`, `check_inline_full_suite.py`, `check_md_line_length.py` and their tests all inspected via raw sed/grep/Read while authoring `commit_preflight.py`, zero graph queries; SWEEP-3 — the two new hook `.py` files and `check_wide_grep.py` reference-read all inspected via raw grep/sed/Read, zero graph queries; BUG-039 daily_snapshot Telegram fix — inspected telegram.py, markdown.py, dhan/positions.py, formatting.py, daily_snapshot.py, test_escaping_guard.py, eod_summary.py via ~7 raw grep/sed calls, never called search_code/get_code_snippet/trace_path; SWEEP-2 — `check_md_line_length.py` and its test Read directly and the new hook `.py` files authored/debugged with zero graph queries (Rule 0 decision tree skipped for `scripts/`) |
| 4 | sequential-single-file-section-reads | After a `grep -n` gives the line numbers of every section you need to edit in one file, fetch them in a single parallel batch of `Read` calls (or one wider range) instead of paging the same file across separate turns. | token-efficiency | 2026-08-27 | 2026-08-28 | RDO-14 — `docs/plan/README.md` read at offsets 214 / 186 / 40 / 27 and `root-doc-organization/tasks.md` at 273 / 1 / 355 across separate turns |
| 3 | codebase-mcp-missing-required-param | Pass `project` (and `repo_path` for `index_repository`) on the first `codebase-memory-mcp` call — omitting them costs a wasted round trip each time. | token-efficiency | 2026-08-27 | 2026-09-01 | RDO-15 — `get_code_snippet("scripts/hooks/check_story_structure.py")` failed once on missing `project` before the real call; ROLL-7 — `get_code_snippet("ReEntryMixin._check_reentry")` and `index_repository` each failed once on a missing `project` / `repo_path` before the real call |
| 3 | askuserquestion-preview-json-parse-fail | Keep `AskUserQuestion` calls to a plain `questions` array of short plain-string fields — no `preview` fields, no `{"raw": …}` envelope; multi-line/backtick/brace content has repeatedly failed the tool's JSON parse and cost a full retry. | token-efficiency | 2026-08-27 | 2026-08-28 | RDO-14 — a `{"raw": "<escaped json>"}` envelope failed "could not be parsed as JSON"; plain `questions` array retry succeeded |
| 3 | clarify-questions-before-context-gather | Read the task's target docs before the first clarifying question so one consolidated ask covers both direction and approach, instead of asking direction first and a second question on the approach after reading. | token-efficiency | 2026-08-27 | 2026-08-27 | /work priority-source fix — asked "where should priority live" (PRIORITY.md / column / row-order) before reading `root-doc-organization/tasks.md`, whose RDO-13 §4 already established TODOS.md stays the canonical list; user's answer matched RDO-13, not any offered option |
| 2 | reflow-interleaved-with-edits | Run the reflow helper once, after all structural Edits to a doc are done — not right after the initial Write with more Edits still to come; each reflow rewrites line breaks and every pending Edit's exact-string anchor goes stale, forcing a re-read. | token-efficiency | 2026-08-29 | 2026-09-03 | SWEEP-1 — reflow run on `suggestions-sweep/stories.md` between cluster-content Edits; later Edits then failed on stale anchors and needed a re-Read; doc-format-migration — reflow_md run on the epic task files and root-doc-organization/tasks.md+stories.md mid-authoring; ~4 Edits then failed on stale anchors and needed sed/Read re-reads |
| 2 | plan-gate-skipped-on-prescriptive-prompt | State the one-sentence plan and wait for go-ahead even when the task prompt is highly prescriptive and touches >2 files — a detailed assignment is not an explicit go-ahead, and CLAUDE.md Step 3 still gates a multi-file change. | protocol-compliance | 2026-09-01 | 2026-09-02 | ROLL-7 — user assigned ROLL-7 with a full bullet list (files, two commits, doc updates); implementation started straight after reading CONTEXT.md with no plan stated, 8 code files ultimately touched; MEAS-2 — `/work` handed off to CLAUDE.md Step 3, 3 files touched (README + tasks.md + TODOS.md), no plan sentence surfaced or go-ahead awaited |
| 2 | next-marker-points-at-just-closed-task | When ticking a `tasks.md` box, set the `docs/plan/README.md` / `TODOS.md` `next:` marker to the next *unchecked* id, not the one just closed — `check_checkbox_consistency.py` flags a marker pointing at a done id. | commit-hygiene | 2026-08-29 | 2026-09-02 | RDO-17.2 — docs commit 4b114ff set the marker to RDO-17.2 as it was being ticked; the hook flagged it and a third commit c3a9179 was needed to move it to RDO-17.3; FIX-2 — docs commit 7d83b2e ticked the FIX-2 box and advanced the epic's own README, but `docs/plan/README.md:17` still reads "next is `fixed-overhead/` **FIX-2**" |
| 2 | staged-index-not-checked-before-commit | Run `git diff --cached --name-only` immediately before every `git commit` — an earlier `git mv` or `git add <dir>` leaves files staged, and a pathspec-less `git commit` then bundles them across phase boundaries. | commit-hygiene | 2026-08-29 | 2026-09-01 | RDO-6 — the first `git commit` for the RDO-7 phase also swept in 3 files left staged by an earlier `git mv` + `git add .agents/skills`; needed `git reset --soft HEAD~1` to unbundle and re-commit cleanly; ROLL-6 — first `git commit` aborted on the pre-session-staged `suggestions.md` (>200-char lines tripped the md hook); had to `git restore --staged` it |
| 2 | context-md-full-sequential-read | Grep `CONTEXT.md` for the task's module/keyword first instead of a blind sequential `Read` from the top — the file is dense enough that a single unscoped read can hit the display token cap before reaching the relevant section. | token-efficiency | 2026-08-27 | 2026-08-27 | root-doc-organization RDO-1 — an `offset 26 limit 40` chunk read hit the 25K cap (28,059 tokens) because lines were 5–18K chars; should have run the `awk` length check first |
| 1 | mirror-doc-path-convention-assumed | Before writing paths into a mirrored protocol doc (`AGENTS.md`, `.agents/skills/**`), grep an existing mirror for the convention first — this repo's mirrors reference `.claude/` paths, not their own directory, and assuming otherwise silently produces dangling pointers on the mirrored surface. | protocol-compliance | 2026-09-02 | 2026-09-02 | FIX-1 — `AGENTS.md` was authored with `.agents/skills/...` pointers; a grep of the pre-change file showed the repo convention is `.claude/skills/...`, needing a sed pass over 6 references before commit (self-caught, but only because a mirror audit happened to run) |
| 1 | next-marker-prose-evades-guard | Write every `docs/plan/README.md` story-row pointer in the literal `next: **<ID>**` form — `check_checkbox_consistency.py`'s `README_ENTRY_RE` matches only that shape, so a row phrased "next is **FIX-2**" is structurally invisible to the stale-marker guard. | protocol-compliance | 2026-09-02 | 2026-09-02 | FIX-2 — `docs/plan/README.md:17` names the just-closed FIX-2 as next, yet `check_checkbox_consistency.py --all` exits 0 because the row says "next is", not "next:" |
| 1 | spec-prestep-skipped-source-not-read | When a task spec lists a "Before any code" pre-step that says to read the source, run it — deciding which doc lines are load-bearing invariants from the doc's own wording alone cannot detect a claim the code no longer supports. | protocol-compliance | 2026-09-02 | 2026-09-02 | FIX-2 — `stories.md` said "Read the target module's code to judge which statements are load-bearing invariants vs. reference narration"; the trim of notifications/portfolio/client was decided from the `CLAUDE.md` prose alone, with zero graph queries and zero source reads |
| 1 | heredoc-rewrite-echoes-whole-file-back | For a partial rewrite of a file already in context, edit it with a targeted `python3` / `sed -i` replace rather than `cat > file <<'EOF'` — a full heredoc rewrite makes the harness echo the entire new file back as a system-reminder, re-paying the whole file in tokens. | token-efficiency | 2026-09-02 | 2026-09-02 | FIX-2 — three `cat >` rewrites of the notifications/portfolio/client `CLAUDE.md` files echoed back ~296 lines (~3.5K tokens); the later targeted `python3` replaces on `stories.md`, `tasks.md` and `TODOS.md` produced no echo-back at all |
| 1 | context-md-ack-not-stated | State "CONTEXT.md ✓" explicitly in the first user-facing response after reading it, per Step 1 — reading the file without the visible acknowledgment leaves no record Step 1 happened even when it did. | protocol-compliance | 2026-09-01 | 2026-09-01 | BUG-039 daily_snapshot Telegram fix — CONTEXT.md head was read via bash sed early in the session but the first user-facing reply never stated "CONTEXT.md ✓" |
| 1 | line-coupled-baseline-set-before-code-frozen | When adding or moving a test-baseline entry keyed by `(file, line)`, freeze the target file first (run `ruff format` + apply every planned edit) and read the final line number once — setting it before the code is frozen means each later reformat/edit shifts the line and re-runs the guard test to failure. | commit-hygiene | 2026-09-01 | 2026-09-01 | ROLL-6 — `_BASELINE_UNESCAPED` entry for `scripts/eod_summary.py` chased the `notifier.send()` line across 4 values (114→193→203→201→200) as ruff-format and a review fix shifted it; 3 extra guard-test reruns |
| 1 | search-graph-broad-query-result-dump | Use a specific `qualified_name` with `get_code_snippet`, or `search_code` with a tight pattern, instead of a multi-word `search_graph` query — a broad query returns 100+ ranked results (thousands of tokens) when you only needed to confirm one symbol exists. | token-efficiency | 2026-09-01 | 2026-09-01 | ROLL-6 — `search_graph('pnl_emoji alert_emoji formatting')` (115 hits) and `search_graph('eod_summary test')` (3769 hits) each dumped ~100 rows just to check whether a helper / a test file already existed |
| 1 | ruff-format-check-skipped-precommit-abort | Run `ruff format --check` (not just `ruff check`) on every new/edited `.py` before `git commit` — the lint pass passing does not mean the format hook will; the pre-commit `ruff format` hook auto-reformats and aborts the commit, costing a re-stage + re-commit cycle. | commit-hygiene | 2026-08-29 | 2026-08-29 | RDO-17.7 — first `git commit` for 526e431 aborted: `ruff format` reformatted `reflow_md.py` + `test_reflow_md.py`; had to `ruff format --check`, re-`git add`, re-commit |
| 1 | py-authored-lines-over-ruff-cap | When authoring a new `.py`, wrap code and docstring lines to the project's ruff line-length as you write instead of discovering a batch of over-cap lines via an `awk 'length>N'` sweep at commit time and fixing them across many small Edits. | token-efficiency | 2026-09-03 | 2026-09-03 | FIX-4 — `scripts/dev/graph_snippet.py` authored with several >88-char lines; ~6 sequential Edits chasing the `awk length > 88` list before the pre-commit ruff pass would go green |
| 1 | full-suite-run-for-docs-only-change | For a docs / tooling-only change, gate on the targeted hook-test dir (`pytest tests/unit/scripts/dev/hooks/ -q`, ~3s) — not `pytest tests/unit/` (~35s) run several times; the full suite touches nothing the diff changed and its xdist flakiness then costs extra confirmatory reruns. | token-efficiency | 2026-08-29 | 2026-08-29 | RDO-17.5 — ran the full unit suite 4× for a zero-code markdown conversion; 2 of those runs were only to confirm 3 xdist-contention failures in test_migrate_exit_events_counterfactual_dte_marks.py were pre-existing flakes |
| 1 | precommit-all-files-mass-reformat | Verify hook changes with `pre-commit run --files $(git diff --cached --name-only)`, never `pre-commit run --all-files` — the pinned ruff/format hooks auto-fix the whole repo, and the blanket `git reset` + `git checkout -- .` recovery also clobbers any intentional `git mv` staging. | commit-hygiene | 2026-08-29 | 2026-08-29 | hook relocation — `pre-commit run --all-files` reformatted ~50 unrelated `src/`/`tests/` files; reverting it unstaged the `git mv scripts/hooks → scripts/dev/hooks` and the deletes had to be re-done with `git rm` |
| 1 | parallel-subagent-ran-git-stash | When spawning parallel file-editing subagents, forbid every index/stash-touching git command explicitly (not just add/commit/stash) and tell them to compute before/after with `awk`/`grep` only — one ran `git stash` for a line count and captured the other agents' concurrent edits. | agent-routing | 2026-08-29 | 2026-08-29 | RDO-6 backlog reflow — 1 of 5 parallel doc-editing subagents ran `git stash` despite an explicit prohibition, pulling the whole concurrent working tree into a stash; recovered via `git stash apply` but left a dangling `stash@{0}` |
| 1 | cd-in-compound-bash-command | Never put `cd` in a compound Bash command — the shell's working directory persists across Bash tool calls, so `cd sub/dir && grep …` silently breaks the next relative-path command. Use absolute paths or `git -C` / `--` path args. | token-efficiency | 2026-08-29 | 2026-08-29 | RDO-17.5 task-def session — `cd docs/plan/telegram-markdown-migration && grep …` left the CWD there; the next `find docs/archive …` failed "No such file" and needed a `cd <repo-root>` reset call |
| 1 | todos-pointer-not-synced-with-readme | When a later commit in the same session changes a story's "next task", update the `TODOS.md` Feature-Backlog pointer + add the session-log line in that same commit — not just `docs/plan/README.md` — or the two disagree until someone notices. | commit-hygiene | 2026-08-29 | 2026-08-29 | RDO-17.5 task-def commit 4d5611a moved the story's next task to RDO-17.5 in `docs/plan/README.md` but left `TODOS.md` Feature-Backlog #1 saying "next RDO-16" and added no session-log entry |
| 1 | bash-isms-in-zsh-shell | The session shell is zsh — don't use bash-only builtins (`mapfile`, `readarray`) or rely on `$var` word-splitting for newline lists; use `while read` / `xargs -0` / explicit arrays, or a one-shot Python filter. | token-efficiency | 2026-08-29 | 2026-08-29 | doc-format-migration — `mapfile -t FILES` (zsh: command not found) then `"${FILES[@]}"` empty; a second attempt passed a newline-joined string as one path → FileNotFoundError traceback; 2 wasted runs |
| 1 | lint-hook-run-outside-configured-scope | Before running a repo lint/format hook manually on a path, check its `.pre-commit-config.yaml` `files:` regex — the path may be out of scope, so a manual run only dumps a pre-existing backlog the commit never touches. | token-efficiency | 2026-08-28 | 2026-08-28 | RDO-15 — ran `check_md_line_length.py docs/archive/TODOS_ARCHIVE.md` (archive is excluded from the `md-line-length` hook); it printed 167 pre-existing over-length lines |
| 1 | transform-script-iterated-not-prototyped | For a bulk mechanical text transform (semantic-linefeed reflow, mass rename), prototype the algorithm on a ~20-line slice or a dry-run diff preview before running it on the full file — don't land it via 4 rewrites of the script each dumping its residual-line list into context. | token-efficiency | 2026-08-28 | 2026-08-28 | RDO-9b — `reflow.py` → `reflow2.py` (buggy `, ` re-append) → `reflow3.py` (too weak) → `reflow4.py` + 3 `TARGET` tuning passes before the DECISIONS.md wrap landed |
| 1 | skill-loaded-then-abandoned | Settle the Claude-vs-Antigravity routing call firmly before invoking `handoff-antigravity` — its `SKILL.md` body (~1.5K tokens) is only useful once the decision is "Antigravity"; loading it then reversing to "Claude implements" is pure waste. | agent-routing | 2026-08-28 | 2026-08-28 | RDO-9b — invoked `handoff-antigravity`, then reconsidered and decided Claude implements (docs-only, judgment-heavy, full file already in context) |
| 1 | plan-file-count-grew-silently | When a stated N-file plan cascades to many more files during execution (a section rename that breaks inbound references, etc.), name the newly-in-scope files in one line before editing them — don't quietly touch 3× the count the user approved. | protocol-compliance | 2026-08-28 | 2026-08-28 | RDO-14 — plan said 4 files; the `## Priority-Ordered Open Work` → `## Feature Backlog` rename pulled in CLAUDE.md, AGENTS.md, 2 skills, 3 backtest-engine docs (12 total), none flagged first |
| 1 | md-hook-backlog-not-checked-before-edit | Before editing any hook-covered `.md` (root, `docs/plan/**`, `docs/bugs/**`), run the `awk 'length>200'` check on it and decide reflow-vs-`SKIP=md-line-length` at plan time — don't discover the pre-existing backlog mid-edit and eat a false-start rewrite. | token-efficiency | 2026-08-28 | 2026-08-28 | RDO-13 — first `docs/plan/README.md` Write still had 17 over-long table rows (redone as a compact list); `CLAUDE.md`/`AGENTS.md` 5a edits written then reverted once the whole-file backlog trip was noticed |
| 1 | scope-question-jumped-ahead | When the user hands you a multi-phase story prompt, confirm scope for the FIRST unstarted phase only — don't bundle a later blocked-phase decision (e.g. RDO-2's AGENTS.md call) into the opening scope gate. | protocol-compliance | 2026-08-27 | 2026-08-27 | root-doc-organization RDO-1 — asked the AGENTS.md delete/stub question before starting RDO-1; user pushed back "did you complete RDO-1 why you went to RDO-2" |
| 1 | scheduleWakeup-poll-spawned-agent | Don't call `ScheduleWakeup` to poll for a subagent you spawned — harness-tracked work re-invokes you automatically via task-notification on completion; polling wastes a turn and reloads per-turn hook overhead for nothing. | token-efficiency | 2026-08-27 | 2026-08-27 | ROLL-4 session — polled twice for the code-reviewer subagent instead of just ending the turn |
| 1 | sequential-tasks-md-reads | Batch multiple candidate `tasks.md`/checklist reads into one parallel tool-call block instead of reading them one at a time across separate turns, when the routing logic only needs to know which one has the first unchecked box. | token-efficiency | 2026-08-27 | 2026-08-27 | ROLL-4 session — read backbone/formatting-rules/strategy-rollout tasks.md sequentially across 3 turns |
| 1 | skill-file-path-assumed-global | Skill `SKILL.md` files in this repo are project-local (`.claude/skills/<name>/SKILL.md` under the repo root), not `~/.claude/`. Check the repo path first — a same-batch `ls .claude/skills/` already shows this — instead of guessing the global path and eating a wrong-path round trip. | token-efficiency | 2026-08-27 | 2026-08-27 | root-doc-organization plan session — 2 wasted `Read` calls locating `md-cleanup/SKILL.md` (global path guess + hook false-positive) |
| 1 | context-md-skipped-on-escalated-ops-task | When an ops/diagnostic task (crontab check, log triage) turns into a `src/` code change mid-session, stop and read `CONTEXT.md` before editing — the protocol's Step 1 applies the moment code is in scope, not just at session start. | protocol-compliance | 2026-08-27 | 2026-08-27 | nuvama session — opened as "which crontab to run", became an `options_reader.py` fix; CONTEXT.md never read |
