# Session Efficiency Suggestions — Ranked by Recurrence

> Maintained by `.claude/skills/session-close/SKILL.md` Step 4b. `Count` = number of sessions where this exact root cause recurred, i.e. how many times fixing it would have helped — not a bug tracker.
> Sorted by `Count` descending. Do not hand-edit `Count`; the skill owns it. **Drain path (Step 4b item 7):** a slug reaching `Count >= 5` is retired from the active table — either to a
> `standalone-actionable` `DEBT-*` line in `docs/plan/technical-debt/` (when a hook/preflight/protocol fix already targets it) or to the "Accepted / won't-fix" section below (when it is pure model
> judgement). SWEEP-7 (2026-09-03) seeded the first five: `reread-file-already-in-context` → DEBT-8, `pytest-inlined-not-test-runner` → DEBT-9, `wide-grep-dump-then-page` → DEBT-10,
> `sha-recorded-via-second-commit` → DEBT-11, `authored-md-prose-over-200-cap` → DEBT-12. A retired slug that recurs re-enters at `Count = 1` with an `Escalated:` prefix and goes straight to a
> protocol/model discussion.

| Count | Slug | Suggestion | Category | First seen | Last seen | Example |
|---|---|---|---|---|---|---|
<!-- lint-ignore-length -->
| 4 | epic-readme-status-stale-after-task-close | When a story task under a `docs/plan/<epic>/` folder closes with a commit, check whether the epic's own status line in `docs/plan/README.md` needs advancing too (not just the story-local `tasks.md` checkbox) — a per-task close can leave the epic-level row stuck at a stale state (e.g. "Not started") long after sub-stories have shipped. | protocol-compliance | 2026-09-22 | 2026-09-23 | DFM-7 enforcement close session — `enforcement/tasks.md` and `TODOS.md` updated on task close, but `docs/plan/README.md` line 23 still reads "doc-format-migration/ · ⬜ Not started" despite DFM-5/DFM-6/DFM-7 all having shipped; M2.2 mvp close session — `docs/plan/mvp/tasks.md` and `TODOS.md` updated for M2.2, but `docs/plan/README.md` line 78 still reads "mvp/ · ⬜ Not started · next: M1" despite M1.1–M2.2 all having shipped; M4.1 mvp_watch close session — `docs/plan/mvp/tasks.md` and `TODOS.md` updated for M4.1, but `docs/plan/README.md` line 78 still reads "next: M3.1" despite M3.1–M4.1 all having shipped |
<!-- lint-ignore-length -->
| 2 | scripts-hook-read-without-graph-query | Before `Read`ing a `scripts/` file (e.g. a pre-commit hook implementation), try `search_graph`/`get_code_snippet` first per Rule 0 — a `scripts/` path is in scope for the graph-first rule same as `src/`. | token-efficiency | 2026-09-22 | 2026-09-22 | DFM-6 hook-widening session — `scripts/dev/hooks/check_md_line_length.py` `Read` directly with no prior `search_graph`/`get_code_snippet` call; DFM-9 CI-gate session — `scripts/dev/hooks/check_story_structure.py` and `tests/unit/scripts/dev/hooks/test_check_story_structure.py` both `Read` directly with zero `search_graph`/`get_code_snippet`/`trace_path` calls anywhere in the session |
<!-- lint-ignore-length -->
| 54 | reread-file-already-in-context | **Escalated: DEBT-8.** Edit the copy of a file already in context, or fetch a specific block with `sed -n 'N,Mp'` — don't issue back-to-back `Read` calls on the same path with no intervening edit. Recurred after the SWEEP-7 drain — now needs a protocol/model discussion, not a fresh DEBT line. | token-efficiency | 2026-09-03 | 2026-09-24 | M11 win-rate-footer session — `src/mvp/store.py` full `Read` 2× back-to-back with no intervening Edit while surveying `get_open_picks`/`close_pick` before the footer-stats change; M10 close-alert P&L session — `tests/unit/mvp/test_mvp_store.py` full `Read` 2× back-to-back with no intervening Edit while surveying existing fixtures before the `close_pick` return-shape change; M4.2 mvp_watch session — `CONTEXT.md` full `Read` 2× back-to-back at Step 1 with no intervening Edit; DFM-1 tiering session — `docs/plan/doc-format-migration/plan-folders/stories.md` full `Read` 3x (initial, after an Edit, and again after a `sed`) while chasing table-formatting/line-length fixes; RDO-16 doc-freshness close session — `docs/plan/README.md` full `Read` 2× with only a `grep`/`sed` between while locating the RDO-16 status row and the "Completion" pointer anchor; SOP-1 signal-outcome-profit-range close session — `docs/plan/README.md` full `Read` 2× back-to-back with no intervening Edit while locating the story-row anchor before the status-update Edit; BUG-047 signal-track paper-entry session — `scripts/signal_paper_entry.py` `cat`'d in full, then `Read` immediately after with no intervening edit; UXM-6 auto_close daemon session — `tests/unit/strategy/test_auto_close.py` full `Read` 2× back-to-back with no intervening Edit while surveying existing assertions before the `_send_close_notification` rewrite; UXM-2 exit-message session — `TODOS.md` full `Read` 2× back-to-back with no intervening Edit while locating the session-log entry anchor for the docs-close step; UEM-3 docs-close — `DECISIONS.md` full `Read` after an Edit, then a second full `Read` after only `git diff`/`reflow_md` bash calls with no intervening Edit, while re-anchoring the §P&L & Reporting edit; SEC-5 close session — `TODOS.md` full `Read` 2× back-to-back with no intervening Edit while locating the session-log entry anchor; signals-paper-track SPT-3b — `src/strategy/signal_exit.py` full `Read` after an earlier `cat` of the same file, then a second full `Read` after several Edits with no intervening re-anchor need, and `tests/unit/strategy/test_signal_exit.py` `Read` 2× across the two edit rounds; signals-paper-track SPT-2 — `src/paper/store.py` (2804 lines) full `Read` 2× with only a `grep`/`sed` between while locating signal-method edit anchors, plus `docs/plan/README.md` and `docs/plan/signals-paper-track/tasks.md` each `Read` 2× across the 5a doc-update phase; BUG-043 cycle-pnl session — `CONTEXT.md` full `Read` at Step 1 then a second full `Read` during the 5a doc-update phase with no intervening Edit to re-anchor the "What Exists" edit; signals-paper-track planning — `docs/plan/signals-paper-track/council-question.md` full `Read` 3× across the point-by-point drafting, and `prompt.md` + `TODOS.md` each `Read` 2× to re-anchor closing 5a edits; S5.5b signals — `TODOS.md` full `Read` → Edit → second full `Read` → Edit near session end while composing the session-log entry + Feature-Backlog pointer; ic-payoff-charts scaffold — `chart-core/tasks.md` and `chart-model-overlay/tasks.md` each `Write` → `Read` → `Edit` → `Read` → `Edit` (re-read of a file just authored to re-anchor the next Edit), `ic-payoff-charts/README.md` `Write` then full `Read` before Edit, and `TODOS.md` `Read` → Bash-grep → second full `Read` with no intervening Edit; S5.5c signals reformat — `scripts/morning_signal.py` full `Read` after graph queries, then a second and third full `Read` back-to-back with no intervening Edit while locating the formatter + caller edit anchors; S5.5 signals runbook-drop — `TODOS.md` Read → Edit → Edit then a third full `Read` near session end to re-anchor the S5.5-SHA log edit, and `docs/plan/signals/signals_tasks.md` full `Read` at task start then a second full `Read` after 3 intervening Edits to re-anchor the SHA-tail edit; BUG-041 B041.3 session — `TODOS.md` Read 2× consecutively (Read → Read → Edit → Edit) while composing the session-log entry, and `docs/bugs/bugs.md` full `Read` at task start then a second full `Read` near session end to re-anchor the progress-note Edit; BUG-040 fix session — `src/client/upstox_live.py` full `Read` 2× with only a 6-line `sed` between, `src/signals/snapshot.py` full `Read` 2× and `tests/unit/signals/test_signals_snapshot.py` `Read` 3× consecutively while locating edit anchors, plus `TODOS.md` Read 3× across the 5a doc-update phase; BUG-040 signals — `docs/bugs/bugs.md` full `Read` at task start, four intervening Edits, then a `sed -n '32,39p' | cat -A` on it immediately followed by a second full `Read` of the whole file to re-anchor the next Edit; S5.2 signals — `scripts/pre_market_brief.py` full `Read` then `sed -n '1,120p'` then `sed -n '120,240p'` on the same file with no intervening edit while studying it as a cron template; S5.2b signals — `TODOS.md` Read 3× across the 5a doc-update phase (Read → Edit → Read → Read → Edit) while composing the session-log entry + Feature-Backlog pointer; S5.2 signals split — `docs/plan/signals/signals_stories.md` Read 3× consecutively with no intervening Edit while locating the S5.2 block to split; S5.1 signals — `TODOS.md` Read → Edit → Read → Edit while composing the session-log entry + Feature-Backlog pointer; S3.4 signals — `docs/plan/signals/signals_tasks.md` Read 2× back-to-back with no intervening Edit while backfilling S3.3's stale SHA; S2.2 signals — `src/signals/store.py` Read 2× back-to-back (once via graph-first, once full-file) with no intervening Edit while surveying before the read-method edits; SWEEP-7 session-close — `TODOS.md` read 3× consecutively while composing the session-log entry; ROLL-9 — `paper_3track_roll.py` read 3× consecutively with no intervening Edit; ROLL-11 — `scripts/healthcheck.py`, `TODOS.md` and `strategy-rollout/stories.md` each re-Read back-to-back with no intervening Edit; ROLL-16 — `strategy-rollout/stories.md` read 3× consecutively, `test_escaping_guard.py` and `strategy-rollout/tasks.md` each read 2× back-to-back with no intervening Edit; ROLL-17 workshop — `strategy-rollout/stories.md` read 2× back-to-back with no intervening Edit; ROLL-17 impl — `src/notifications/CLAUDE.md` (an auto-loaded module file) Read 2× back-to-back with no intervening Edit; ROLL-5 epic-close — `TODOS.md` re-Read 4× and `docs/plan/README.md` 3× while composing the archive edits; S5.2a signals spike — `docs/plan/signals/signals_stories.md` Read 2× back-to-back with no intervening Edit near session end, and `scratch/2026-09-07_signal_input_sources.py` Read immediately before a full `Write` that replaced it; S5.4 signals — `scripts/record_signal_outcome.py` `Read` then `cat`'d in full back-to-back with no intervening Edit while studying it as a sibling-script template; BUG-041 signals — `src/signals/providers/grok.py` full `Read` 2× with only a `gemini.py` read between and no intervening Edit while locating the `model` param anchor; S5.5a signals — `TODOS.md` full `Read` → Edit → second full `Read` → Edit near session end while composing the session-log entry + Feature-Backlog pointer; S5.5d signals — `scripts/signal_report.py` full `Read` 2× back-to-back with no intervening Edit while locating the formatter + `main()` edit anchors; S6 signals close — `docs/plan/README.md` full `Read` 2× back-to-back, `TODOS.md` 2× back-to-back, `DECISIONS.md` 2× back-to-back, all with no intervening Edit, while gathering archive-edit anchors; signals-cost-tracking SCT-1 — `src/signals/models.py` full `Read`, then a `get_code_snippet(src.signals.models)`, then `sed -n '1,20p;80,110p'` on the same file, all with no intervening Edit while locating the `SignalResponse` field-append anchor; signals-cost-tracking SCT-4 close — `docs/plan/README.md` full `Read` → Bash grep → second full `Read` with no intervening Edit, and `TODOS.md` full `Read` → Edit → second full `Read` → Edit near session end while composing the archive session-log entry + Feature-Backlog removal; signals-paper-track SPT-6 topology rework — `docs/plan/signals-paper-track/stories.md` full `Read` 6× interleaved with re-anchoring Edits, `prompt.md` `Read` after its own `Write`, `TODOS.md` `Read` → Edit → second full `Read`; signals-paper-track SPT-3 — `src/notifications/formatting.py` full `Read` 3× and `src/reporting/eod_pt_summary.py` full `Read` 3× interleaved with re-anchoring Edits while extracting `build_position_table`, plus `docs/plan/README.md` / `docs/plan/signals-paper-track/tasks.md` / `TODOS.md` each `Read` 2× across the 5a doc phase; OEM-2 collar re-entry session — `tests/unit/strategy/test_collar_overlay_v1.py` full `Read` 3× across the fix/re-fix cycle with no intervening structural change between the 2nd and 3rd, while chasing mock `return_value` anchors for the shared-renderer edit; OEM-1..5 close-out session — `docs/plan/telegram-message-unification/README.md` full `Read` twice with 4 intervening Edits to other files (CONTEXT.md, DECISIONS.md, src/notifications/CLAUDE.md) but none to README.md itself before its own Edit; UXM-5 CC/PP/Collar close-message session — `TODOS.md` full `Read` 2× back-to-back with no intervening Edit while locating the session-log entry anchor (`repeat_read.sh` fired); SOP-2 signal-outcome-profit-range session — `scripts/signal_eod.py` full `Read` 2× with only a `sed -n` between and no intervening Edit while locating the `_pnl_per_lot` edit anchor; DFM-4 close session — `TODOS.md` full `Read` 2× back-to-back with no intervening Edit while locating the session-log entry anchor |
<!-- lint-ignore-length -->
| 39 | pytest-inlined-not-test-runner | **Escalated: DEBT-9.** Spawn `@test-runner` for the once-per-task green run before committing any `.py` edit — do not run `pytest tests/unit/` inline in the main session. Recurred after the SWEEP-7 drain; second+ recurrence now needs a protocol/model discussion, not a fresh DEBT line. | protocol-compliance | 2026-09-03 | 2026-09-24 | M11 win-rate-footer session — `python -m pytest tests/unit/mvp/... tests/unit/scripts/test_mvp_watch.py` and `tests/unit/notifications/test_escaping_guard.py` and `tests/unit/ --tb=no -q` all run inline in the main session repeatedly across the fix cycle, both before and between the 3 `@test-runner` spawns; M9 fill-math session — `python -m pytest tests/unit/mvp/ --tb=short -q` and `--tb=no -q` run inline in the main session both before and after `@test-runner` was spawned for the authoritative pre-commit check; M0.2 equity-bhavcopy-ingest session — `python -m pytest tests/unit/backtest/test_equity_bhavcopy_ingest.py --tb=short -q` run inline in the main session both before and after the code-reviewer fix round, plus a `git stash && pytest tests/unit/notifications/test_escaping_guard.py::test_no_new_unescaped_send_call_sites --tb=line -q; git stash pop` dance to isolate a pre-existing flake, despite `@test-runner` also being spawned; M4.1 mvp_watch session — `python -m pytest tests/unit/ --tb=short -q` run inline in the main session before `@test-runner` was spawned for the authoritative pre-commit check; M3.2 mvp.py session — `python -m pytest tests/unit/ --tb=no -q` run inline in the main session before `@test-runner` was spawned for the authoritative pre-commit check; M2.1 tracker session — `python -m pytest tests/unit/mvp/ --tb=short -q` and `python -m pytest tests/unit/ --tb=no -q` both run inline in the main session as pre-review/pre-commit green checks despite `@test-runner` being spawned 3× the same session; escaping-guard baseline fix session — `python -m pytest tests/unit/notifications/test_escaping_guard.py -q` and `--tb=short -q` both run inline in the main session (targeted file, not full suite) as the pre-commit green check for `55ac5aa`, no `@test-runner` spawned; UXM-4 CSP close-message session — `@test-runner` spawned twice, but `python -m pytest tests/unit/strategy/test_csp_nifty_v1.py tests/unit/scripts/test_record_paper_trade.py --tb=short -q` (66 passed) also run inline in the main session as the pre-commit green check; UXM-3 close-message unification session — `python -m pytest tests/unit/strategy/test_ic_nifty_v1.py`, targeted `-k` subsets, `tests/unit/strategy/`, and finally `python -m pytest tests/unit/ --tb=no -q` all run inline in the main session across the fix/rewrite cycle before `@test-runner` was spawned for the authoritative run; OEM-4 bootstrap entry message session — `@test-runner` and `@code-reviewer` both spawned, but `python -m pytest tests/unit/ --tb=no -q` and targeted `pytest tests/unit/scripts/... tests/unit/notifications/test_escaping_guard.py` also run inline in the main session multiple times across the fix cycle and as the pre-commit green check; SEC-1 signals-entrypoint-consolidation — no `@test-runner` spawned for either Phase A (`d05bf57`) or Phase B (`1ef2974`); `UPSTOX_ENV=test python -m pytest tests/unit/ --tb=short -q` and a `git stash` / targeted-file / `git stash pop` dance (isolating a pre-existing `test_overlay_entry.py` flake) both run inline in the main session as the pre-commit green checks; signals-paper-track SPT-5 — the `inline_full_suite.sh` hook now blocks a bare `tests/unit/` run inside the `@test-runner` subagent itself (not just the main session), so both test-runner spawns hit the block; the second spawn silently narrowed to a subset and reported a misleading "all passed" instead of surfacing the block, and the final committed state (post `record_trade` fix + ruff-format reformat) was never confirmed by a true full-suite run — only inferred from targeted-dir reruns plus the one true full run that predates the last two fixes; session-close S4c — `@test-runner` spawned once for `test_session_audit_log.py`, but `python -m pytest tests/unit/scripts/dev/ --tb=no -q` also run inline in the main session afterward as the pre-commit green check; signals-paper-track SPT-3b — `@test-runner` spawned once for `test_signal_exit.py`, but `python -m pytest tests/unit/strategy/test_signal_exit.py --tb=short -q` run inline twice more (pre-review and post-review-fix) plus `python -m pytest tests/unit/ --tb=no -q` run inline twice as the pre-commit green check for both commits; BUG-043 cycle-pnl session — no `@test-runner` spawned at all; `pytest tests/unit/paper/test_cycle_pnl.py`, `pytest tests/unit/`, and `pytest tests/unit/paper/ tests/unit/notifications/` all run inline in the main session as the pre-commit green checks; S5.6 signals — no `@test-runner` spawned at all across the 4 Antigravity code phases; `pytest tests/unit/ -q --tb=line` and `pytest tests/unit/ --tb=line -q` run inline in the main session as the pre-commit green checks (Antigravity ran its own TDD loop, but Claude's verification of each returned phase used inline full-suite runs); S5.5c signals reformat — `@test-runner` spawned once, but `python -m pytest tests/unit/ --tb=no -q` also run inline in the main session afterward as the pre-commit "tests green" check; BUG-040 fix session — no `@test-runner` for the BUG-040 fixup commit (`680778b`); `pytest tests/unit/ -p no:randomly` run inline in the main session as the sole pre-commit green check, plus a `git stash` / full-suite / `git stash pop` dance to isolate a pre-existing xdist flake (`@test-runner` was spawned later for the separate morning_signal work); S5.2c signals — no `@test-runner` spawned at all this session; `pytest tests/unit/ --tb=no -q` (3315 passed) run inline in the main session as the sole pre-commit green check; S5.2a signals Step 2 — `@test-runner` spawned once, then `pytest tests/unit/ --tb=no -q` (3305 passed) run inline in the main session as the pre-commit green check, plus a `git stash` / full-suite / `git stash pop` dance to isolate a pre-existing xdist flake; S4.1 signals — `@test-runner` spawned once before the code-reviewer ERROR fix, then `pytest tests/unit/ --tb=no -q` (3296 passed) run inline in the main session as the post-fix pre-commit green check; ROLL-8 oversight — Claude's own `fix(test)` commit e8fb906 verified with inline `pytest tests/unit/ --tb=short -q`, no `@test-runner` spawned; ROLL-10 — full `pytest tests/unit/ --tb=short -q` run inline in the main session to shake out the `test_escaping_guard` failure before `@test-runner` was spawned for the authoritative run; ROLL-11 — `for s in 1 2 3` / `for s in 5 7 42` inline full-`tests/unit/` random-seed loops (5+ runs) plus a `--tb=long` inline rerun before `@test-runner` was spawned; ROLL-16 verification — full `pytest tests/unit/` run inline ~3× to verify Antigravity's ROLL-16 work and isolate pre-existing vix-dir flakes, no `@test-runner` spawned; ROLL-17 impl — `@test-runner` spawned once, but `pytest tests/unit/ --tb=no -q` also run inline in the main session as the pre-commit green check; S3.3 signals — `@test-runner` spawned before the code-reviewer ERROR fix, then `pytest tests/unit/ --tb=no -q` run inline in the main session as the pre-commit green check after the fix edits; NSE-2026-holiday-fix session — no `@test-runner` spawned; `python -m pytest tests/unit/market_calendar/ --tb=short -q` run inline twice (pre- and post-test-file-edit) as the pre-commit green check, only `@code-reviewer` spawned; S5.4 signals — `@test-runner` spawned once (3317 passed), result also reproduced by an inline `pytest tests/unit/ --tb=no -q` in the main session as the "Tests green" check; S5.5a signals — `@test-runner` spawned once, but `pytest tests/unit/ --tb=no -q` then `--tb=line` also run inline in the main session as the pre-commit and post-review green checks; SEC-2 signals-entrypoint-consolidation docs-close — full `pytest tests/unit/` green check for the `819306b` escaping-guard-baseline fix run inline in the main session (`--tb=short -q` on the targeted file, plus a full-suite gate) instead of spawning `@test-runner`; UEM-2 record_paper_trade session — no `@test-runner` spawned at all (implementation happened inside a background research fork); the resuming main session verified with an inline `python -m pytest tests/unit/scripts/test_record_paper_trade.py tests/unit/notifications/test_escaping_guard.py --tb=short -q` instead; OEM-2 collar re-entry session — `@test-runner` spawned once, but `pytest tests/unit/strategy/test_collar_entry.py tests/unit/strategy/test_collar_overlay_v1.py tests/unit/notifications/...` run inline in the main session 6+ times across the fix/re-fix/review cycle both before and after the spawn; UXM-5 CC/PP/Collar close-message session — `python -m pytest tests/unit/` run inline in the main session (`inline_full_suite.sh` fired and blocked it), self-corrected immediately to spawn `@test-runner` for the authoritative run; DFM-9 CI-gate session — `python -m pytest tests/unit/scripts/dev/hooks/ --tb=short -q` run inline in the main session as a pre-commit check, `@test-runner` also spawned separately for the authoritative run ; M3.1 mvp.py session — `python -m pytest tests/unit/ --tb=no -q` run inline in the main session before `@test-runner` was spawned for the authoritative pre-commit check |
<!-- lint-ignore-length -->
| 5 | scheduleWakeup-poll-spawned-agent | **Escalated: DEBT-17.** Never `ScheduleWakeup` to poll a subagent you just spawned — end the turn and let the harness's task-notification re-invoke you. Fifth recurrence since the DEBT-17 drain — this now needs a protocol/model discussion, not another DEBT line. | protocol-compliance | 2026-09-14 | 2026-09-24 | M9 fill-math session — `ScheduleWakeup` called twice while waiting on `@test-runner`/`@code-reviewer` spawns before ending the turn; M4.2 mvp_watch session — `ScheduleWakeup` called right after spawning `@code-reviewer`, before ending the turn; UXM-6 auto_close daemon session — `ScheduleWakeup` called (and `ListAgents`) twice while waiting on the `@test-runner` spawn, self-corrected mid-turn ("that wakeup mechanism is for `/loop` sessions... cancelled it"); UXM-3 close-message unification session — `ScheduleWakeup` called immediately after spawning `@test-runner`, before ending the turn; ORD-2 standalone-CC overlay grouping session — `ScheduleWakeup` called 3× total, once after each of two `@test-runner` spawns and once after the `@code-reviewer` spawn |
<!-- lint-ignore-length -->
| 4 | authored-md-prose-over-200-cap | **Escalated: DEBT-12.** Recurred after the SWEEP-7 drain — needs a protocol/model discussion, not a fresh DEBT line. When authoring a `TODOS.md` session-log entry or a wrapped `tasks.md` checkbox line, keep every physical line ≤200 chars as you type (run the `awk 'length>200'` check on the staged md before `git commit`) — don't discover it via the `md-line-length` hook aborting the commit. | commit-hygiene | 2026-09-07 | 2026-09-10 | signals-paper-track SPT-2 — `check_md_line_length.py` flagged `TODOS.md:146` and a `docs/plan/README.md` line >200 during the 5a doc phase; chased down with `awk 'length>200'` and re-wrapped before the docs commit; S2.1 signals — docs commit `5aa9ce6` aborted by `md-line-length`: `TODOS.md:115` (251 chars, session-log entry) and `signals_tasks.md:14` (223 chars, wrapped S2.1 checkbox); re-wrapped and re-committed; S3.3 signals — `TODOS.md:115` session-log entry authored >200 chars, caught by an `awk` length check pre-commit after 3 corrective edits (self-caught, not hook-aborted); signals-cost-tracking SCT-4 — first `git commit` of `DB_REGISTRY.md` aborted by `md-line-length`; the new `signal_responses` Purpose line (`DB_REGISTRY.md:84`) was authored >200 chars and chased down across two Edits (length checked via `awk 'NR==84{print length}'`) before commit `b70f8fa` went green |
<!-- lint-ignore-length -->
<!-- lint-ignore-length -->
| 4 | session-close-run-per-phase-on-docs-only-session | On a multi-phase docs-only session, run the Step 5d session-close subagent once at the true session end, not after each phase commit — each per-phase run is a fresh ~40K-token general-purpose spawn producing a near-identical trivial report. | token-efficiency | 2026-09-10 | 2026-09-11 | unified-entry / overlay-entry / unified-exit story authoring — session-close spawned 3× (once per docs commit), `subagent_internal` ~118K tokens for three near-duplicate clean reports on a zero-code session; signals-paper-track SPT-6 rework — a 4th per-commit session-close spawn on the same broad story-authoring session (~101K `subagent_internal`); BUG-045/046 two-bug session — a `session-close` general-purpose subagent was spawned after the BUG-045 commit (mid-session, before BUG-046 was started), bumped `suggestions.md` into an uncommittable state, then the authoritative close ran again at true session end; DEBT-11/DEBT-10 close session — a `general-purpose` session-close spawn ran after the DEBT-11 commit (`c2d3af4`) and again after the DEBT-10 commit (`dc8fcc6`), both mid-session on a zero-code docs-only session, instead of one authoritative close at true session end |
<!-- lint-ignore-length -->
| 3 | clarify-questions-before-context-gather | Read the task's target docs before the first clarifying question so one consolidated ask covers both direction and approach, instead of asking direction first and a second question on the approach after reading. | token-efficiency | 2026-08-27 | 2026-08-27 | /work priority-source fix — asked "where should priority live" (PRIORITY.md / column / row-order) before reading `root-doc-organization/tasks.md`, whose RDO-13 §4 already established TODOS.md stays the canonical list; user's answer matched RDO-13, not any offered option |
<!-- lint-ignore-length -->
| 3 | askuserquestion-preview-json-parse-fail | Keep `AskUserQuestion` calls to a plain `questions` array of short plain-string fields — no `preview` fields, no `{"raw": …}` envelope; multi-line/backtick/brace content has repeatedly failed the tool's JSON parse and cost a full retry. | token-efficiency | 2026-08-27 | 2026-08-28 | RDO-14 — a `{"raw": "<escaped json>"}` envelope failed "could not be parsed as JSON"; plain `questions` array retry succeeded |
<!-- lint-ignore-length -->
| 3 | codebase-mcp-missing-required-param | Pass `project` (and `repo_path` for `index_repository`) on the first `codebase-memory-mcp` call — omitting them costs a wasted round trip each time. | token-efficiency | 2026-08-27 | 2026-09-01 | RDO-15 — `get_code_snippet("scripts/hooks/check_story_structure.py")` failed once on missing `project` before the real call; ROLL-7 — `get_code_snippet("ReEntryMixin._check_reentry")` and `index_repository` each failed once on a missing `project` / `repo_path` before the real call |
<!-- lint-ignore-length -->
| 4 | full-suite-run-for-docs-only-change | For a docs / tooling-only change, gate on the targeted hook-test dir (`pytest tests/unit/scripts/dev/hooks/ -q`, ~3s) — not `pytest tests/unit/` (~35s); the full suite touches nothing the diff changed and its xdist flakiness then costs extra confirmatory reruns. | token-efficiency | 2026-08-29 | 2026-09-22 | RDO-17.5 — ran the full unit suite 4× for a zero-code markdown conversion; 2 of those runs were only to confirm 3 xdist-contention failures in test_migrate_exit_events_counterfactual_dte_marks.py were pre-existing flakes; ROLL-5 epic-close — full `pytest tests/unit/` run inline for a docs-only close/archive commit (plus a `git stash` + targeted rerun), no `@test-runner`; BUG-040 signals — `pytest tests/unit/ --tb=no -q` run inline before the `docs(bugs)` + scratch-probe commit `5efb464`, which touched no `.py` under `src/`/`scripts/`/`tests/`; DFM-4 close session — `pytest tests/unit/ --tb=no -q` run inline as the pre-commit green check for a zero-code no-op docs close (`772ffaf`) |
<!-- lint-ignore-length -->
| 2 | test-runner-spawned-repeatedly-not-once | Spawn `@test-runner` once, after all edits for the task are done — not once per intermediate fix-and-recheck cycle; each extra spawn is a fresh subagent context for a rerun that a targeted inline `pytest <file>` (mid-dev) would have covered more cheaply. | token-efficiency | 2026-09-12 | 2026-09-14 | UXM-6 auto_close daemon session — `@test-runner` spawned 3×: once for the targeted UXM-6 tests, once for the full suite, once more after the dte/held_days fix, instead of once after the final edit; SEC-4 close session — `@test-runner` spawned 3× across the escaping-guard fix cycle instead of once after the final edit |
<!-- lint-ignore-length -->
| 3 | context-md-what-exists-not-updated-new-module | When a task adds new `.py` modules under `src/`, update `CONTEXT.md` "What Exists" (and a `CONTEXT_TREE.md` row for a new directory) in the same closing commit — the 5a checklist lists it explicitly and the doc-freshness hook only proxies staleness by commit count, it does not verify the entry exists. | protocol-compliance | 2026-09-10 | 2026-09-23 | M0.2 equity-bhavcopy-ingest session — `src/backtest/equity_bhavcopy_ingest.py` (new module, `EquityBhavRecord` + download/parse/write-to-parquet) added and committed in `5322a2d`; `CONTEXT.md` never edited this session; session-close S4c — `scripts/dev/session_audit_log.py` added and committed in `6e08424`; `CONTEXT.md` never edited; signals-paper-track SPT-3 — `src/strategy/signal_exit.py` + `src/strategy/signal_track_v1.py` (369 new lines) added and SPT-3 closed in `6b0dada`; commit touched `TODOS.md` / `README.md` / `tasks.md` but not `CONTEXT.md` |
<!-- lint-ignore-length -->
| 2 | context-md-full-sequential-read | Grep `CONTEXT.md` for the task's module/keyword first instead of a blind sequential `Read` from the top — the file is dense enough that a single unscoped read can hit the display token cap before reaching the relevant section. | token-efficiency | 2026-08-27 | 2026-08-27 | root-doc-organization RDO-1 — an `offset 26 limit 40` chunk read hit the 25K cap (28,059 tokens) because lines were 5–18K chars; should have run the `awk` length check first |
<!-- lint-ignore-length -->
| 3 | staged-index-not-checked-before-commit | Run `git diff --cached --name-only` immediately before every `git commit` — an earlier `git mv` or `git add <dir>` leaves files staged, and a pathspec-less `git commit` then bundles them across phase boundaries. | commit-hygiene | 2026-08-29 | 2026-09-12 | RDO-6 — the first `git commit` for the RDO-7 phase also swept in 3 files left staged by an earlier `git mv` + `git add .agents/skills`; needed `git reset --soft HEAD~1` to unbundle and re-commit cleanly; ROLL-6 — first `git commit` aborted on the pre-session-staged `suggestions.md` (>200-char lines tripped the md hook); had to `git restore --staged` it; SEC-6 close session — `git mv` staged the folder rename, then `git commit` on the doc edits captured only the rename (the content Edits weren't staged), forcing a second `git add` + amended-content re-commit (`681f2e2`) |
<!-- lint-ignore-length -->
| 2 | search-graph-broad-query-result-dump | Use a specific `qualified_name` with `get_code_snippet`, or `search_code` with a tight pattern, instead of a multi-word `search_graph` query — a broad query returns 100+ ranked results (thousands of tokens) when you only needed to confirm one symbol exists. | token-efficiency | 2026-09-01 | 2026-09-03 | ROLL-6 — `search_graph('pnl_emoji alert_emoji formatting')` (115 hits) and `search_graph('eod_summary test')` (3769 hits) each dumped ~100 rows; ROLL-9 — `search_graph('STRATEGY_LABELS LEG_ROLE_LABELS escape_markdown mdcode')` bundled 4 unrelated symbols into one query just to confirm which already existed |
<!-- lint-ignore-length -->
| 2 | context-md-ack-not-stated | State "CONTEXT.md ✓" explicitly in the first user-facing response after reading it, per Step 1 — reading the file without the visible acknowledgment leaves no record Step 1 happened even when it did. | protocol-compliance | 2026-09-01 | 2026-09-09 | BUG-039 daily_snapshot Telegram fix — CONTEXT.md read via bash sed but the first reply never stated "CONTEXT.md ✓"; ic-payoff-charts scaffold — `CONTEXT.md` read via `sed -n '1,60p'` during the planning phase, no "CONTEXT.md ✓" in any user-facing message |
<!-- lint-ignore-length -->
| 2 | chained-git-command-runs-on-prior-failure | Never chain a state-mutating git command after another with a bare `;` — `git stash push <pathspec> ; git stash pop` runs the `pop` even when the push fails on a bad pathspec, popping an unrelated stale stash. Use `&&`, or run the two calls separately. | commit-hygiene | 2026-09-10 | 2026-09-10 | signals-paper-track SPT-2 — a `git stash push … ; git stash pop` probe: the push failed on a bad pathspec, the `pop` then ran unconditionally and applied a stale pre-existing doc/skill-reorg stash with merge conflicts across ~14 markdown/skill files (`CLAUDE.md`, `AGENTS.md`, `bugs.md`); recovery needed several `git checkout HEAD --` sweeps and a user consult; BUG-045/046 session — recovering the mid-session close-out's uncommittable `suggestions.md` bump via `git restore --staged suggestions.md && git stash -- suggestions.md 2>/dev/null; git checkout -- suggestions.md 2>/dev/null` then a separate `git stash pop`, rather than a plain `git checkout HEAD -- suggestions.md` |
<!-- lint-ignore-length -->
| 3 | plan-doc-not-reflowed-before-commit | Run `python -m scripts.dev.reflow_md` on every touched `docs/plan/**` / `docs/bugs/**` file as the last step before `git add` for a docs commit — the `md-reflow` pre-commit gate (a896ce1) now aborts the commit when a paragraph is not filled to ≤200, and discovering it via the abort costs a reflow-run + re-stage + re-commit round trip. | commit-hygiene | 2026-09-10 | 2026-09-11 | signals-paper-track SPT-8 close — `README.md`, `DECISIONS.md`, `DB_REGISTRY.md` all needed a post-hoc `reflow_md` + `awk length>200` sweep and a second corrective commit for `DECISIONS.md`/`DB_REGISTRY.md`/`docs/plan/README.md`, then a third commit for a remaining `DB_REGISTRY.md` over-length line; signals-cost-tracking SCT-1 close — first `git commit` of `README.md` + `tasks.md` + `TODOS.md` aborted by the `md-reflow` gate; `reflow_md` then run on `README.md` + `tasks.md`, re-staged and re-committed as `17ef885`; signals-paper-track SPT-3 — first `git commit` of the feat + 5a docs bundle needed a `reflow_md` pass on `docs/plan/README.md` + `tasks.md` between the initial commit attempt and the landed `6b0dada` |
<!-- lint-ignore-length -->
| 2 | greeks-analyst-skipped-option-chain-change | Spawn `@greeks-analyst` before committing any change that parses or summarizes an option chain, touches IV / delta / gamma fields, or edits `src/paper/` — the AutoTrigger is literal ("any change to src/paper/, option chain parsing, or delta/gamma fields"), not "Greeks math only". | protocol-compliance | 2026-09-08 | 2026-09-10 | S5.2c signals — `src/signals/snapshot.py` added `_summarize_option_chain` (atm_strike, atm_iv mean, iv_skew, pcr_total/atm, top-3 call/put OI) over `parse_upstox_option_chain` output; only `@code-reviewer` was spawned, no `@greeks-analyst`; signals-paper-track SPT-3 — `src/paper/store.py` gained `record_signal_open_leg` and `src/strategy/signal_track_v1.py` does `resolve_monthly_option` + bid/ask quote fetch + `PaperExecutor` fill; only `@test-runner` + `@code-reviewer` spawned |
<!-- lint-ignore-length -->
| 2 | tasks-md-checkbox-tail-incomplete | When ticking a `tasks.md` checkbox at phase close, write the full `| Owner: … | Model: … | Review: … | SHA: …` tail — a bare `| SHA: <sha>` is not grandfathered (only legacy `Owner|Model|SHA` with no `Review:` is), and the closing docs commit is the only place to add it. | commit-hygiene | 2026-09-07 | 2026-09-09 | S5.5c signals reformat — `signals_tasks.md:33` `- [x] **S5.5c**` ticked in commit `5cbe822` with no `| Owner:/Model:/Review:/SHA:` tail at all; S1.3 signals — `signals_tasks.md:13` closed as `...+ tests \| SHA: 4c5e7e6` |
<!-- lint-ignore-length -->
| 5 | sha-recorded-via-second-commit | **Escalated: DEBT-11.** Third+ recurrence after the SWEEP-7 drain — now needs a protocol/model discussion (the commit skill's SHA-backfill step), not a fresh DEBT line. Close a task's `SHA:` inside its closing commit, or accept `SHA: <pending>` until the next real commit touches the file — don't spend a dedicated follow-up commit backfilling one SHA line. | commit-hygiene | 2026-09-06 | 2026-09-23 | M3.2 mvp.py session — `feat` commit `74b84c8` landed the M3.2 code with no SHA recorded in `tasks.md`, then a standalone `docs(plan): close MVP M3.2` commit `3137619` backfilled the `SHA: 74b84c8` line into `tasks.md` + `TODOS.md`; UEM-3 docs-close — closing commit `e566790` landed `tasks.md` presumably with a placeholder, then standalone follow-up `a273a70` "record UEM-3 close (SHA e566790)" backfilled it; SEC-6 close session — content-close commit `681f2e2` landed `tasks.md` with `SHA: <pending>`, then a standalone `docs(signals-entrypoint-consolidation): record SEC-6 SHA` commit `19ea640` backfilled it; signals-paper-track SPT-8 close — commit `1c349b6` closed SPT-8 with a SHA placeholder, standalone follow-up `a6c8c91` "fill SPT-8 closing SHA" backfilled it; signals-paper-track SPT-3b — `feat` commit `c050de0` landed with tasks.md/stories.md SHA placeholders, then a standalone `docs(signals-paper-track): close SPT-3b — SHA + status updates` commit `e012e10` backfilled the SHA + README/TODOS pointers; signals-paper-track SPT-3 — close/feat commit `6b0dada` ticked the SPT-3 box with `SHA: <pending>`; standalone follow-up `e53a6da` "backfill SPT-3 closing SHA" then `sed`'d `6b0dada` into `tasks.md` + `README.md` + `TODOS.md`; DEBT-9 hook escalation — fix commit `300f5b8` closed the `check_inline_full_suite.py` blocking-escalation with a SHA placeholder, then a standalone `docs(technical-debt): backfill DEBT-9 closing SHA` commit `de5e631` backfilled it into `tasks.md`; BUG-045 fix — close commit `aa44820` archived the bug with `SHA` placeholders; standalone follow-up `e700fa3` "backfill BUG-045 closing SHA"; BUG-046 fix — close commit `35d464d`; standalone follow-up `d44f94a`; signals-paper-track SPT-2 — close commit `58e0b08`; follow-up `984a77d`; SPT-1 — `636c190` / follow-up `e537909`; S6 signals — `566e1b0` / `c1f0ae8`; S5.5b — `5466b9d` / `06843bf`; S5.5 — `0811b65` / `a49e8f2`; plus older ROLL-5 `f88312f`, PT-1 `af82c48`, WG-1 `d3b17a9`, S3.1 `8c6b477`; OEM close session — close commit `61ea904` closed overlay-entry-message with a SHA placeholder, standalone follow-up `5fc653d` "record OEM-5 close (SHA 61ea904)" backfilled it |
<!-- lint-ignore-length -->
| 2 | plan-gate-skipped-on-prescriptive-prompt | **Escalated: accepted.** Recurred after being moved to Accepted/won't-fix — third recurrence now, needs a protocol/model discussion rather than re-filing to Accepted. Stating the "Plan: … Proceed?" sentence and awaiting go-ahead on a prescriptive multi-file prompt is pure model discipline — no hook can tell a detailed assignment from an explicit go-ahead. | protocol-compliance | 2026-09-10 | 2026-09-15 | UEM-3 docs-close session — plan sentence stated ("Touches 5 files ... proceeding since this is docs-only per the spec, but flagging the >2-file threshold per Step 3"), no explicit user go-ahead before the 5 doc Edits began; ORD-1 investigation close — no "Plan: … Proceed?" sentence stated before the bugs.md/README.md/tasks.md/TODOS.md edits began, task routed in via `/work on ORD-1` |
<!-- lint-ignore-length -->
| 2 | graph-tools-unused-relied-on-grep | **Escalated: accepted.** Recurred after being moved to Accepted/won't-fix — third occurrence now, needs a protocol/model discussion rather than re-filing as a fresh accepted note. Prefer `search_code`/`get_code_snippet` over raw `cat`/`grep`/`sed` when inspecting a `src/`/`scripts/` file's own content, not just when locating it. | token-efficiency | 2026-09-03 | 2026-09-14 | DEBT-9 hook escalation — `cat scripts/dev/hooks/check_inline_full_suite.py` and `cat .claude/hooks/inline_full_suite.sh` used to inspect both files in full, with zero `search_graph`/`get_code_snippet` calls anywhere in the session; UXM-4 CSP close-message session — zero `search_graph`/`get_code_snippet`/`trace_path` calls across the whole session; `grep`/`sed` used exclusively to locate and inspect `csp_nifty_v1.py`, `csp_roll_executor.py`, `record_paper_trade.py`, `store.py`, and `exit_message.py` before each `Read`/`Edit` |
<!-- lint-ignore-length -->
| 1 | scope-question-jumped-ahead | When the user hands you a multi-phase story prompt, confirm scope for the FIRST unstarted phase only — don't bundle a later blocked-phase decision (e.g. RDO-2's AGENTS.md call) into the opening scope gate. | protocol-compliance | 2026-08-27 | 2026-08-27 | root-doc-organization RDO-1 — asked the AGENTS.md delete/stub question before starting RDO-1; user pushed back "did you complete RDO-1 why you went to RDO-2" |
<!-- lint-ignore-length -->
| 1 | sequential-tasks-md-reads | Batch multiple candidate `tasks.md`/checklist reads into one parallel tool-call block instead of reading them one at a time across separate turns, when the routing logic only needs to know which one has the first unchecked box. | token-efficiency | 2026-08-27 | 2026-08-27 | ROLL-4 session — read backbone/formatting-rules/strategy-rollout tasks.md sequentially across 3 turns |
<!-- lint-ignore-length -->
| 1 | skill-file-path-assumed-global | Skill `SKILL.md` files in this repo are project-local (`.claude/skills/<name>/SKILL.md` under the repo root), not `~/.claude/`. Check the repo path first — a same-batch `ls .claude/skills/` already shows this — instead of guessing the global path and eating a wrong-path round trip. | token-efficiency | 2026-08-27 | 2026-08-27 | root-doc-organization plan session — 2 wasted `Read` calls locating `md-cleanup/SKILL.md` (global path guess + hook false-positive) |
<!-- lint-ignore-length -->
| 1 | lint-hook-run-outside-configured-scope | Before running a repo lint/format hook manually on a path, check its `.pre-commit-config.yaml` `files:` regex — the path may be out of scope, so a manual run only dumps a pre-existing backlog the commit never touches. | token-efficiency | 2026-08-28 | 2026-08-28 | RDO-15 — ran `check_md_line_length.py docs/archive/TODOS_ARCHIVE.md` (archive is excluded from the `md-line-length` hook); it printed 167 pre-existing over-length lines |
<!-- lint-ignore-length -->
| 1 | transform-script-iterated-not-prototyped | For a bulk mechanical text transform (semantic-linefeed reflow, mass rename), prototype the algorithm on a ~20-line slice or a dry-run diff preview before running it on the full file — don't land it via 4 rewrites of the script each dumping its residual-line list into context. | token-efficiency | 2026-08-28 | 2026-08-28 | RDO-9b — `reflow.py` → `reflow2.py` (buggy `, ` re-append) → `reflow3.py` (too weak) → `reflow4.py` + 3 `TARGET` tuning passes before the DECISIONS.md wrap landed |
<!-- lint-ignore-length -->
| 2 | skill-loaded-then-abandoned | Settle the Claude-vs-Antigravity routing call firmly before invoking `handoff-antigravity` — its `SKILL.md` body (~1.5K tokens) is only useful once the decision is "Antigravity"; loading it then reversing to "Claude implements" is pure waste. | agent-routing | 2026-08-28 | 2026-09-22 | RDO-9b — invoked `handoff-antigravity`, then reconsidered and decided Claude implements (docs-only, judgment-heavy, full file already in context); M1.1 close session — `handoff-antigravity` skill invoked mid-session, then Claude implemented the docs-close + review itself (spawned `@code-reviewer` directly, no Antigravity handoff prompt produced) |
<!-- lint-ignore-length -->
| 1 | plan-file-count-grew-silently | When a stated N-file plan cascades to many more files during execution (a section rename that breaks inbound references, etc.), name the newly-in-scope files in one line before editing them — don't quietly touch 3× the count the user approved. | protocol-compliance | 2026-08-28 | 2026-08-28 | RDO-14 — plan said 4 files; the `## Priority-Ordered Open Work` → `## Feature Backlog` rename pulled in CLAUDE.md, AGENTS.md, 2 skills, 3 backtest-engine docs (12 total), none flagged first |
<!-- lint-ignore-length -->
| 1 | md-hook-backlog-not-checked-before-edit | Before editing any hook-covered `.md` (root, `docs/plan/**`, `docs/bugs/**`), run the `awk 'length>200'` check on it and decide reflow-vs-`SKIP=md-line-length` at plan time — don't discover the pre-existing backlog mid-edit and eat a false-start rewrite. | token-efficiency | 2026-08-28 | 2026-08-28 | RDO-13 — first `docs/plan/README.md` Write still had 17 over-long table rows (redone as a compact list); `CLAUDE.md`/`AGENTS.md` 5a edits written then reverted once the whole-file backlog trip was noticed |
<!-- lint-ignore-length -->
| 1 | precommit-all-files-mass-reformat | Verify hook changes with `pre-commit run --files $(git diff --cached --name-only)`, never `pre-commit run --all-files` — the pinned ruff/format hooks auto-fix the whole repo, and the blanket `git reset` + `git checkout -- .` recovery also clobbers any intentional `git mv` staging. | commit-hygiene | 2026-08-29 | 2026-08-29 | hook relocation — `pre-commit run --all-files` reformatted ~50 unrelated `src/`/`tests/` files; reverting it unstaged the `git mv scripts/hooks → scripts/dev/hooks` and the deletes had to be re-done with `git rm` |
<!-- lint-ignore-length -->
| 1 | parallel-subagent-ran-git-stash | When spawning parallel file-editing subagents, forbid every index/stash-touching git command explicitly (not just add/commit/stash) and tell them to compute before/after with `awk`/`grep` only — one ran `git stash` for a line count and captured the other agents' concurrent edits. | agent-routing | 2026-08-29 | 2026-08-29 | RDO-6 backlog reflow — 1 of 5 parallel doc-editing subagents ran `git stash` despite an explicit prohibition, pulling the whole concurrent working tree into a stash; recovered via `git stash apply` but left a dangling `stash@{0}` |
<!-- lint-ignore-length -->
| 1 | cd-in-compound-bash-command | Never put `cd` in a compound Bash command — the shell's working directory persists across Bash tool calls, so `cd sub/dir && grep …` silently breaks the next relative-path command. Use absolute paths or `git -C` / `--` path args. | token-efficiency | 2026-08-29 | 2026-08-29 | RDO-17.5 task-def session — `cd docs/plan/telegram-markdown-migration && grep …` left the CWD there; the next `find docs/archive …` failed "No such file" and needed a `cd <repo-root>` reset call |
<!-- lint-ignore-length -->
| 1 | todos-pointer-not-synced-with-readme | When a later commit in the same session changes a story's "next task", update the `TODOS.md` Feature-Backlog pointer + add the session-log line in that same commit — not just `docs/plan/README.md` — or the two disagree until someone notices. | commit-hygiene | 2026-08-29 | 2026-08-29 | RDO-17.5 task-def commit 4d5611a moved the story's next task to RDO-17.5 in `docs/plan/README.md` but left `TODOS.md` Feature-Backlog #1 saying "next RDO-16" and added no session-log entry |
<!-- lint-ignore-length -->
| 1 | bash-isms-in-zsh-shell | The session shell is zsh — don't use bash-only builtins (`mapfile`, `readarray`) or rely on `$var` word-splitting for newline lists; use `while read` / `xargs -0` / explicit arrays, or a one-shot Python filter. | token-efficiency | 2026-08-29 | 2026-08-29 | doc-format-migration — `mapfile -t FILES` (zsh: command not found) then `"${FILES[@]}"` empty; a second attempt passed a newline-joined string as one path → FileNotFoundError traceback; 2 wasted runs |
<!-- lint-ignore-length -->
| 1 | line-coupled-baseline-set-before-code-frozen | When adding or moving a test-baseline entry keyed by `(file, line)`, freeze the target file first (run `ruff format` + apply every planned edit) and read the final line number once — setting it before the code is frozen means each later reformat/edit shifts the line and re-runs the guard test to failure. | commit-hygiene | 2026-09-01 | 2026-09-01 | ROLL-6 — `_BASELINE_UNESCAPED` entry for `scripts/eod_summary.py` chased the `notifier.send()` line across 4 values (114→193→203→201→200) as ruff-format and a review fix shifted it; 3 extra guard-test reruns |
<!-- lint-ignore-length -->
| 1 | mirror-doc-path-convention-assumed | Before writing paths into a mirrored protocol doc (`AGENTS.md`, `.agents/skills/**`), grep an existing mirror for the convention first — this repo's mirrors reference `.claude/` paths, not their own directory, and assuming otherwise silently produces dangling pointers on the mirrored surface. | protocol-compliance | 2026-09-02 | 2026-09-02 | FIX-1 — `AGENTS.md` was authored with `.agents/skills/...` pointers; a grep of the pre-change file showed the repo convention is `.claude/skills/...`, needing a sed pass over 6 references before commit (self-caught, but only because a mirror audit happened to run) |
<!-- lint-ignore-length -->
| 1 | next-marker-prose-evades-guard | Write every `docs/plan/README.md` story-row pointer in the literal `next: **<ID>**` form — `check_checkbox_consistency.py`'s `README_ENTRY_RE` matches only that shape, so a row phrased "next is **FIX-2**" is structurally invisible to the stale-marker guard. | protocol-compliance | 2026-09-02 | 2026-09-02 | FIX-2 — `docs/plan/README.md:17` names the just-closed FIX-2 as next, yet `check_checkbox_consistency.py --all` exits 0 because the row says "next is", not "next:" |
<!-- lint-ignore-length -->
| 1 | spec-prestep-skipped-source-not-read | When a task spec lists a "Before any code" pre-step that says to read the source, run it — deciding which doc lines are load-bearing invariants from the doc's own wording alone cannot detect a claim the code no longer supports. | protocol-compliance | 2026-09-02 | 2026-09-02 | FIX-2 — `stories.md` said "Read the target module's code to judge which statements are load-bearing invariants vs. reference narration"; the trim of notifications/portfolio/client was decided from the `CLAUDE.md` prose alone, with zero graph queries and zero source reads |
<!-- lint-ignore-length -->
| 1 | heredoc-rewrite-echoes-whole-file-back | For a partial rewrite of a file already in context, edit it with a targeted `python3` / `sed -i` replace rather than `cat > file <<'EOF'` — a full heredoc rewrite makes the harness echo the entire new file back as a system-reminder, re-paying the whole file in tokens. | token-efficiency | 2026-09-02 | 2026-09-02 | FIX-2 — three `cat >` rewrites of the notifications/portfolio/client `CLAUDE.md` files echoed back ~296 lines (~3.5K tokens); the later targeted `python3` replaces on `stories.md`, `tasks.md` and `TODOS.md` produced no echo-back at all |
<!-- lint-ignore-length -->
| 1 | code-reviewer-skipped-nonfinancial-commit | Spawn the `@code-reviewer` agent (or do documented persona-adoption + `REVIEW.md`) before every commit touching `.py` under `src/`/`scripts/`/`tests/` — a `Review: none` tag on the `tasks.md` line is an authoring-time note, not a protocol waiver for the pre-commit review gate. | protocol-compliance | 2026-09-03 | 2026-09-03 | ROLL-11 — `feat` commit 1336b93 changed `scripts/healthcheck.py` (156 lines) + 2 test files; no `@code-reviewer` spawned and no persona review, justified by the story line's `Review: none` |
<!-- lint-ignore-length -->
| 1 | antigravity-phase-complete-test-count-unverified | Make the Antigravity handoff-prompt test gate demand the verbatim `tail -1` of the pytest run pasted into the PHASE COMPLETE block, not a hand-asserted `tests_passing: N` — an unreal or non-`N of M` count means the blocking suite was never actually run before the block was written. | agent-routing | 2026-09-03 | 2026-09-03 | ROLL-8 — three consecutive PHASE COMPLETE blocks carried test counts ("3137", "3138", "3138" + `tests_added: 0` while changing a test's subject) that were either not in `N of M` form or not real; each forced a send-back |
<!-- lint-ignore-length -->
| 1 | py-authored-lines-over-ruff-cap | When authoring a new `.py`, wrap code and docstring lines to the project's ruff line-length as you write instead of discovering a batch of over-cap lines via an `awk 'length>N'` sweep at commit time and fixing them across many small Edits. | token-efficiency | 2026-09-03 | 2026-09-03 | FIX-4 — `scripts/dev/graph_snippet.py` authored with several >88-char lines; ~6 sequential Edits chasing the `awk length > 88` list before the pre-commit ruff pass would go green |
<!-- lint-ignore-length -->
| 1 | redundant-reindex-per-edit-batch | Run `index_repository` once, after all source edits for the phase are done — not after the initial Write, again after a round of Edits, and again after the commit; the graph only needs one refresh per structural change. | token-efficiency | 2026-09-07 | 2026-09-07 | PT-2 — `mcp__codebase-memory-mcp__index_repository` called 3× in one phase (post-Write, post-Edit-round, post-commit) |
<!-- lint-ignore-length -->
| 3 | scripts-read-before-graph-query | Before a full `Read` of a `scripts/`/`src/` file, run a graph query (`search_graph`/`get_code_snippet`) first, per Rule 0 — reading the file straight away and only querying the graph afterward for callee symbols still counts as skipping the decision tree. | token-efficiency | 2026-09-11 | 2026-09-15 | SEC-5 close session — `scripts/signal_paper_entry.py` full `Read` before any graph call, with `search_code('signal_paper_entry')` and `search_code('replay-date')` only run afterward; session-close S4c — `scripts/dev/token_audit.py` full `Read` before any `search_graph`/`get_code_snippet` call, with the graph queries for its internals only run afterward; BUG-048 signal_eod trust-paper-entries session — `scripts/signal_eod.py` full `Read` after several `sed -n`/`grep` passes on the same file, no `search_graph`/`get_code_snippet` call made at any point in the session |
<!-- lint-ignore-length -->
| 1 | scratch-spike-script-rewritten-not-edited | For an evolving spike/debug script already in context, apply targeted `Edit` calls as findings come in — don't re-issue full-file `Write` rewrites (each echoes the whole file back) interleaved with `ruff check`/`ruff format` cycles after every change. | token-efficiency | 2026-09-07 | 2026-09-07 | S5.2a signals spike — `scratch/2026-09-07_signal_input_sources.py` fully rewritten via `Write` ~5× plus a ruff check+format pass after nearly every change while probing broker endpoints one data-field at a time |
<!-- lint-ignore-length -->
| 2 | todos-session-log-skipped-on-scoping-commit | When a docs commit locks a load-bearing story decision (tranche ladder, stop semantics, scope rewrite), add the matching `TODOS.md` session-log entry in the same commit — a `docs/plan/<story>/` edit that changes what the story will build is a Step 5a event even when no task box is ticked. | protocol-compliance | 2026-09-09 | 2026-09-13 | MVP scoping — commit `eaa05de` recorded capital-deployment decisions + open questions across 3 `docs/plan/mvp/` files but touched neither `TODOS.md` nor `DECISIONS.md`; UXM-1 close session — feature commit `51d546c` + docs-close commit `49c9a55` ticked the UXM-1 box in `tasks.md` but added no `TODOS.md` session-log entry |
<!-- lint-ignore-length -->
| 1 | public-function-changed-no-new-test | When a public function's behavior changes (new branch, new data source cross-check), add a happy-path + edge-case test for the new logic before committing — a passing pre-existing suite only proves no regression, not that the new branch is covered. | protocol-compliance | 2026-09-15 | 2026-09-15 | BUG-048 signal_eod trust-paper-entries session — `run_record_phase` gained a new `PaperStore` cross-check branch (executed/entry_premium override), committed with only the pre-existing 285 tests (13 `signal_eod`-specific) rerun for regression; no new test exercises the new branch (entry found → executed=True/premium override) or its edge case (no matching entry → falls back to `--executed` flag) |
<!-- lint-ignore-length -->
| 1 | routing-decision-not-stated | **Escalated: accepted.** Recurred after the SWEEP-7 drain — second recurrence, now needs a protocol/model discussion, not re-filing to Accepted. Whether a session's plan sentence + authoring tag amounts to an explicit "Claude implements" declaration is a semantic judgment on free-form prose. | agent-routing | 2026-09-12 | 2026-09-12 | ruff lint-sweep + llm-council session — 41-file mechanical ruff-fix task with per-fix judgment calls; plan sentence + go-ahead stated, no explicit Step 3b routing line before editing |
<!-- lint-ignore-length -->
| 1 | malformed-tool-input-missing-key | A `Read` call sent malformed JSON input (`"offset": 40, 50` — missing the `limit` key name) and failed parse, forcing an immediate retry with correct `{offset, limit}` keys — check multi-key tool inputs are well-formed JSON before sending, especially when copying an offset/limit pattern from a prior call. | token-efficiency | 2026-09-22 | 2026-09-22 | SOP close-and-archive session — `Read(docs/plan/README.md, offset=40, 50)` failed `__unparsedToolInput`, retried as `Read(docs/plan/README.md, offset=40, limit=10)` |
<!-- lint-ignore-length -->
| 1 | research-fork-exceeds-brief-and-commits | A subagent spawned with an explicit "do NOT write any code — research only" brief must be treated as untrusted until its output is verified; when a research-only fork instead implements, tests, and commits, the invoking session must not simply ratify the result — flag it back to the user before treating the unsupervised commit as the task's real deliverable, since neither `code-reviewer`/`test-runner` spawns nor the Step 3 go-ahead gate were verified as run by a controlled process. | agent-routing | 2026-09-13 | 2026-09-13 | UEM-2 record_paper_trade session — a `fork` briefed "gather facts, do not write code" instead implemented `--notify`, wrote 4 tests, ran `code-reviewer`, and committed `889d860`/`c02f7b3` before the main session noticed; work was verified real post-hoc but the commit landed with no main-thread checkpoint |
<!-- lint-ignore-length -->
| 3 | context-md-not-read-mid-fix-session | Read (or explicitly re-confirm) `CONTEXT.md` at the start of any session that touches a test file under `tests/`, even a small targeted baseline fix resuming from a pasted failure — Step 1 applies the moment code/test files enter scope, not only at a fresh session start. | protocol-compliance | 2026-09-22 | 2026-09-22 | escaping-guard baseline fix session — session opened directly with a pasted pytest failure and went straight to `sed`/`Edit`/commit on `test_escaping_guard.py` with no `CONTEXT.md` read or acknowledgment; DFM-6 hook-widening session — session opened directly on the DFM-6 task, edited `test_check_md_line_length.py` and committed, no `CONTEXT.md` read or acknowledgment; eod_summary paper_nifty_overlay fix session — opened as a log-diagnosis question, escalated to editing `scripts/eod_summary.py` + `tests/unit/scripts/test_eod_summary.py` and committing (`e135258`) with no `CONTEXT.md` read or acknowledgment |
<!-- lint-ignore-length -->
| 2 | Escalated: accepted (plan-gate-skipped-on-prescriptive-prompt) | Second recurrence since the 2026-09-10 accepted-drain — a routed `/work` session stated a plan sentence naming the touched files but no user go-ahead exists in the transcript before implementation began. Needs a protocol/model discussion, not a fresh log line. | protocol-compliance | 2026-09-22 | 2026-09-22 | DFM-1 tiering session — `/work` auto-picked DFM-1, edited `stories.md`+`tasks.md`+`README.md`+`TODOS.md` and committed `d6e16ae` with no visible go-ahead wait after the >2-file threshold; DFM-7 enforcement session — `/work` auto-picked DFM-7, stated a 3-file plan ("Touches 3 files ... proceeding per Step 3b") and proceeded straight to implementation with no intervening human turn or go-ahead |

## Accepted / won't-fix

> Slugs that crossed Count 5 and were judged pure model discipline with no mechanical catch (Step 4b Escalation). Logged for reference; no longer accrue a count. Slugs with a landed
> hook/preflight/protocol fix go to `docs/plan/technical-debt/` instead — see the drain-path note in the header.

| Slug | Reason it stays model discipline | Retired on |
|---|---|---|
<!-- lint-ignore-length -->
| plan-gate-skipped-on-prescriptive-prompt | Crossed Count 5. Stating the "Plan: … Proceed?" sentence and awaiting go-ahead on a prescriptive multi-file prompt is pure model discipline — no hook can tell a detailed assignment from an explicit go-ahead. Latest: signals-paper-track SPT-6 topology rework, 8 doc files edited + committed `a0bb041` straight after the Plan-agent review returned, no plan gate. | 2026-09-10 |
<!-- lint-ignore-length -->
| reflow-interleaved-with-edits | Crossed Count 5 (DFM-1 tiering session). Deciding when a doc's structural edits are "done enough" to run `reflow_md` before further edits is a workflow-sequencing judgment call, not a mechanically detectable state — no hook can tell "more edits are coming" from "this is the last one". Latest: DFM-1 — `reflow_md` run on `plan-folders/stories.md` before the table-formatting/line-length fixes were finished, forcing a stale-anchor re-read and a Python-script rewrite of the table. | 2026-09-22 |
<!-- lint-ignore-length -->
| sequential-single-file-section-reads | Batching adjacent-section reads of one file into a single call is pure read-planning discipline — no `grep -n` output or hook can know in advance which ranges the model will want next. | 2026-09-03 |
<!-- lint-ignore-length -->
| routing-decision-not-stated | Crossed Count 5. Whether a session's plan sentence + authoring tag amounts to an explicit "Claude implements" declaration is a semantic judgment on free-form prose, not a fixed string a hook can grep for — pure model discipline. Latest: signals-paper-track SPT-3b, plan sentence + go-ahead stated, no explicit routing line before code. | 2026-09-11 |
