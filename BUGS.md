# NiftyShield — Known Bugs (moved)

The bug registry no longer lives at the repo root. Confirmed defects in shipped code are
tracked in [`docs/bugs/`](docs/bugs/). The original flat registry (only `BUG-001` still
open) is archived at [`docs/archive/BUGS_LEGACY.md`](docs/archive/BUGS_LEGACY.md).
