# NiftyShield — CONTEXT.md "What Exists" (verbatim snapshot, 2026-08-27)

> Archived from `CONTEXT.md` on 2026-08-27 (story `root-doc-organization`, RDO-1) to cut the
> always-loaded session cost. This section had grown into ~15 run-on paragraphs (one line was
> 18,740 chars) that duplicated `CONTEXT_TREE.md` (file-level module tree) and `DECISIONS.md`
> (feature/bugfix history). The live equivalents:
> - **[CONTEXT_TREE.md](../../CONTEXT_TREE.md)** — what each module/file does
> - **[DECISIONS.md](../../DECISIONS.md)** — every BUG-*/SNAP-*/PG-* entry cited below, with rationale
>
> This file is a frozen record. Do not edit. Nothing here was deleted — it was relocated.

---

## Current State (as of 2026-08-26)

### What Exists (committed and working)

Full file-level module tree: **[CONTEXT_TREE.md](CONTEXT_TREE.md)**
Load that file when adding new modules or doing a full structural survey.
Key top-level packages: `src/auth`, `src/client`, `src/models`, `src/portfolio`, `src/paper`, `src/mf`, `src/dhan`, `src/nuvama`, `src/intraday`, `src/instruments`, `src/market_calendar`, `src/notifications`, `src/utils`, `src/backtest`, `src/risk`, `src/gamma`, `src/strategy`, `src/council`, `src/db.py`
`src/risk/` — portfolio-level delta risk controls. `PortfolioDelta` frozen dataclass (`src/risk/models.py`): `options_delta_lots`, `niftybees_delta_lots`, `total_delta_lots`, `warning_breached`, `cap_breached`, `as_of`. `PortfolioDeltaTracker` (`src/risk/delta_tracker.py`): `aggregate_delta(paper_positions, nifty_spot, lot_size, position_deltas=None) → PortfolioDelta`; options-only thresholds warning=0.75/cap=1.0 lots, combined thresholds warning=1.5/cap=2.0 lots; parameterised via constructor. Classification is by `PaperPosition.option_type` (not `instrument_key` substring — fixed in BUG-002/B002.3, real Upstox keys are numeric-only). If `position_deltas` (dict, `instrument_key` → signed delta-in-lots) supplies a chain-derived value for a PE/CE leg, that value is used as-is (B002.4); otherwise falls back to the approximation CE=`net_qty/lot_size`, PE=`-net_qty/lot_size` with a logged WARNING (never silent — module stays pure/zero-I/O per council ruling `docs/council/2026-07-02_paper-delta-source-architecture.md`, caller is responsible for resolving the map). FUT = `net_qty/lot_size`; NiftyBees = `qty×avg_cost/(spot×lot_size)`; unresolved `option_type` → WARNING + zero delta (never misclassified as a future). `check_entry_allowed` (`src/risk/entry_gate.py`): protective entries always allowed; cap → block; warning → allow with message. 33 unit tests in `tests/unit/risk/test_delta_tracker.py` + hypothesis property tests in `tests/unit/risk/test_delta_hypothesis.py`.
`src/gamma/` — scaffolding, data models (`GammaChainSnapshot` and `GammaWatchlistEntry` frozen dataclasses), and persistence (`GammaStore` SQLite operations) for Near-Expiry Gamma Buy strategy.
`src/backtest/ivr.py` — `compute_ivr(vix_today, vix_series)`: IVR formula over trailing 252-day VIX window; returns `float | None`; clamps to `[0.0, 1.0]`; flat-window safe (returns 0.5). 11 unit tests in `tests/unit/backtest/test_ivr.py`.
`src/backtest/vix_ingest.py` — India VIX ingestion pipeline. Supports NSE CSV (legacy) and Upstox API.canonical Parquet storage: `data/historical/ohlc/india_vix/`. Resumable — identifies gaps and fetches missing days. 7 unit tests in `tests/unit/backtest/test_vix_ingest.py`.
`src/backtest/chain_writer.py` — `ChainWriter` class for EOD and intraday chain snapshot Parquet writes. Both `write_eod_snapshot`/`write_intraday_snapshot` take a `label` param (e.g. "monthly"/"quarterly"/"yearly", default `"default"`) appended to the output filename — required to disambiguate multiple expiries snapshotted in the same run; without it, same-timestamp writes for different expiries silently overwrote each other (BUG-006, fixed SHA 7e0801c). 14 unit tests in `tests/unit/backtest/test_chain_writer.py`.
`src/backtest/chain_reader.py` — `ChainReader` class for DuckDB-based option chain EOD and intraday query scan utilities. 8 unit tests in `tests/unit/backtest/test_chain_reader.py`.

`src/models/options.py` — `OptionLeg`, `OptionChainStrike`, `OptionChain` (all `frozen=True` Pydantic). Source-agnostic field names; Upstox parser in `src/client/upstox_market.py` (`parse_upstox_option_chain`). Dhan parser not implemented (chain-data story complete; Dhan approach not pursued).
`src/paper/` — paper trading module. `TradeState` enum (`OPEN`/`DEFENDED`/`RE_ENTRY_PENDING`) in `models.py`. `PaperTrade` model (frozen Pydantic, `paper_` prefix enforced, includes `ivr_at_entry: float | None`, `state: TradeState = OPEN`), `PaperPosition` + `PaperNavSnapshot` + `PaperLegSnapshot` (frozen dataclasses) — `PaperPosition.option_type: Literal["PE","CE","FUT","EQ"] | None` (B002.3) resolved lazily by `PaperStore.get_position`/`get_positions` via `InstrumentLookup`, not stored; `NIFTYBEES_KEY` short-circuits to `"EQ"`; falls back to `None` (logged WARNING, never raises) on unresolved key, missing/corrupt BOD JSON, or a resolved `instrument_type` outside CE/PE/FUT; also `None` with no lookup attempted at all when the leg is flat (`net_qty == 0`) — BUG-014 fix, 2026-07-20 — since a closed leg's `instrument_key` references a settled/delisted contract that can never resolve again once it drops out of the BOD file, and resolving unconditionally produced a permanent per-run warning. `PaperStore` constructor takes an optional `instrument_lookup: InstrumentLookup | None` param for this (lazily built from `DEFAULT_BOD_PATH` if not injected; not thread-safe). `PaperStore` (`paper_trades` + `paper_nav_snapshots` + `paper_leg_snapshots` + `pending_approvals` + `council_outputs` + `daemon_heartbeat` + `paper_exit_events` tables in shared SQLite), `PaperTracker` (compute_pnl + record_daily_snapshot + **`compute_pnl_by_leg_group`**, 2026-08-02 — groups a strategy's open/closed leg_roles via `OVERLAY_LABELS` (`src/paper/_display.py`) into display groups, combining `overlay_collar_call`+`overlay_collar_put` into one "Collar" entry since they're always traded as a pair; unrealized/realized per group computed by reusing the existing per-leg helpers, not a new calculation. `scripts/portfolio/paper_snapshot.py` calls this instead of `compute_pnl` specifically for `STRATEGY_OVERLAY` ("paper_nifty_overlay"), so the EOD table prints one row per overlay type (`paper_nifty_overlay / CC`, `/ PP`, `/ Collar`) instead of one blended row — persistence to `paper_nav_snapshots` is unchanged, still one aggregate row per strategy via the untouched `record_daily_snapshot`; this only changes what's printed). `get_positions(strategy_name)` groups by `(strategy_name, leg_role, instrument_key)` — one `PaperPosition` per instrument, not per leg_role, filtered to `net_qty != 0` (PG-1, 2026-07-25) — a roll's SELL on the expiring instrument no longer nets against the replacement instrument's BUY under the same leg_role. `get_position(strategy_name, leg_role, instrument_key=None)` — filters to the exact `(leg_role, instrument_key)` pair when given; when omitted and multiple positions share a leg_role (roll overlap), picks the most-recent `entry_date` and logs `paper_store.get_position_ambiguous` WARNING (PG-2a). **SNAP-5, 2026-08-07:** `PaperStore.record_nav_snapshot()` now enforces the same write-time `total_pnl == unrealized_pnl + realized_pnl` invariant `record_leg_snapshot()` already had (raises `ValueError` on mismatch). Root cause of the 42 historical violations: `generate_track_snapshot` (`src/paper/track_snapshot.py`) computed `net_pnl` via `_normalize_overlay_pnls()`, which drops `overlay_cc`'s contribution when `overlay_collar_call` is also open (same contract, two roles) — but `total_unrealized`/`total_realized` were accumulated without that same dedup. Fixed by tracking per-leg-role `overlay_unrealized`/`overlay_realized` dicts and replaying the identical drop rule against the totals before returning `TrackPnL`; `net_pnl`/max-DD/return-on-NEE are numerically unchanged. `scripts/dev/backfill_nav_total_pnl.py` (one-off, mirrors `migrate_paper_trades_state.py`) corrected the 42 pre-existing bad rows in `data/portfolio/portfolio.sqlite` — `total_pnl = Decimal(unrealized_pnl) + Decimal(realized_pnl)`, no trade replay/LTP refetch needed. See `DECISIONS.md` 2026-08-07 SNAP-5.
`PaperStore` API: `record_leg_snapshot` (upsert; enforces `total_pnl == unrealized_pnl + realized_pnl`), `get_leg_snapshot`, `get_prev_leg_snapshot`, `delete_trade` (no-op if missing; WHERE includes `instrument_key` to prevent wrong-row deletion during rolls), `delete_trade_by_id(trade_id)` (preferred rollback primitive when PaperTrade object unavailable), `create_exit_event` (upsert by trade_id + exit_signal), `get_open_exit_events` (filter by strategy/leg/status), `update_trade_state(trade_id, TradeState)` (raises ValueError if id missing), `record_gate_violation(GateViolation) -> int` / `get_gate_violation_counts(strategy_name=None, gate_name=None) -> list[dict]` (new `gate_violations` SQLite table, indexed on `(strategy_name, gate_name)`; `get_gate_violation_counts` pre-aggregates via `GROUP BY` — never returns raw rows, per Rule 1) — `GateViolation` frozen Pydantic model (`gate_name`, `threshold`, `actual`, `strategy_name`, `logged_at`) records a THRESHOLD IC entry gate (IVR floor, DTE window, liquidity floor, portfolio-delta cap) that would have blocked entry under `--log-only-gates` mode (default True in `paper_ic_entry.py`/`_v2.py`); STRUCTURAL gates (duplicate entry, `_post_expiry_gate`, unresolved instrument, stale/missing VIX window) never produce a `GateViolation` and always hard-block — see `DECISIONS.md` 2026-07-03, `get_proxy_delta_breach_count(strategy_name) -> int` / `set_proxy_delta_breach_count(strategy_name, count)` — persist consecutive days the Proxy deep ITM call delta has been below 0.40 in `paper_strategies` table (`proxy_delta_breach_count INTEGER DEFAULT 0`); reset to 0 on delta recovery. `paper_exit_events` schema captures: strategy_name, leg_name, trade_id, exit_signal (`ExitSignal` enum), severity (INFO/WARN/ACTION), ltp/mid/bid/ask/delta/dte, entry_price, threshold_value, dual-signal audit fields (`delta_stop_would_fire`, `premium_stop_would_fire`, `actual_rule_used`), status (OPEN/ACKNOWLEDGED/ACTED/DISMISSED), and `counterfactual_dte_marks` (nullable JSON blob for DTE/delta states at ACTION-severity exits without historical chain reconstruction, populated by IC check_signals). `PaperExitEvent` frozen Pydantic model in `src/paper/models.py`. See `src/paper/CLAUDE.md` for full invariants. **`TrackComparisonSnapshot`** frozen dataclass (`src/paper/models.py`, added 3track-consolidation S3, 2026-07-29) — daily base-leg-only RQ1 comparison row: `strategy_name` (one of the three 3-track strategy names, or synthetic `"nifty_index"` for the Nifty spot series), `snapshot_date`, `pnl_1d_abs`/`pnl_1d_pct` (denominator = yesterday's base-leg mark value, LTP × qty), `pnl_inception_abs`/`pnl_inception_pct` (denominator = entry cost basis, avg_cost × qty — deliberately different from the 1-day denominator), `tracking_error_pct` (track's inception return % minus Nifty spot's return % since entry; `None` for the spot row itself). Persisted in new `paper_track_comparison_snapshots` table (`PRIMARY KEY (strategy_name, snapshot_date)`) via `PaperStore.record_track_comparison_snapshot()` (upsert) / `get_track_comparison_snapshots(strategy_name, start_date=None, end_date=None)` (ordered ASC). Computed in `scripts/strategies/three_track/paper_3track_snapshot.py` by `_compute_track_comparison_snapshot()` (reads only the base leg via `store.get_leg_snapshot`/`get_prev_leg_snapshot` — overlay legs never enter this calculation, by construction) and `_compute_spot_comparison_snapshot()` (4th synthetic series, spot price history sourced from `paper_nav_snapshots.underlying_price` rather than a second table); both called from `_run()` only when `save=True`. Overlay P&L is unaffected and continues to live in `paper_leg_snapshots`/`get_strategy_realized_pnl` — this table never joins or aggregates overlay `leg_role`s. `scripts/dev/generate_3track_viz.py` reads this table for a visually separate "RQ1 Comparison" section, never merged into the overlay-inclusive base/cc/pp/collar series. See `docs/plan/3track-consolidation/stories.md` S3.
`src/strategy/` — paper-backbone strategy layer. `PaperStrategy` protocol (`protocol.py`) + `SignalEvent` + `ApprovedAction` + `LegSpec` models. `ReEntryMixin` (`reentry_mixin.py`): three-gate re-entry check (DTE ≥ 14, IVR ≥ 0.25, no open position); inherited by `CSPNiftyV1`, `CCOverlayV1`, and `CollarOverlayV1`. `StrategyMonitor` (`monitor.py`): registry + tick loop + signal routing + heartbeat + auto-execute dispatch path (strategies with `auto_execute=True` bypass Telegram approval and execute immediately via `PaperExecutor`). **WARN Telegram dedup (2026-08-06):** `_route_event`'s WARN branch no longer sends unconditionally every tick — it checks `PaperStore.is_warn_active(strategy_name, event_type, leg_role, expiry)` and only sends on the OFF→ON transition; `_tick` accumulates a per-strategy `warn_fired: set[(event_type, leg_role, expiry)]` across all expiry groups and calls `PaperStore.reconcile_warn_state()` once per strategy per tick to clear any previously-active condition that didn't fire (recovery → next re-breach alerts immediately). `expiry` (`chain.expiry.isoformat()`) is part of the dedup key so two expiry groups sharing a `leg_role` under one `strategy_name` never alias. No cooldown timer, no escalation tier — operator-chosen design, see DECISIONS.md 2026-08-06. New `warn_signal_state` SQLite table (PK `strategy_name, event_type, leg_role, expiry`) + `is_warn_active`/`set_warn_active`/`reconcile_warn_state` in `src/paper/store.py`. `_log_live_pnl_diag` (2026-07-23, BUG-019, TEMP diagnostic): called at the end of every `_tick()`, restricted to the 15:20-15:30 IST window; for every strategy with an open leg, calls `PaperTracker.compute_pnl()` — the exact function `paper_snapshot.py`'s EOD cron uses — and logs `strategy_monitor.live_pnl_diag`, so the live near-close reading can be diffed against the EOD snapshot's own log line for a suspected disparity, across all strategies not just `paper_ic_nifty_v2_monthly` (BUG-018). `StrategyMonitor.__init__` now constructs `self._tracker = PaperTracker(store, broker)`. `PaperExecutor` (`executor.py`): action dispatch + `PaperFillSimulator` (VIX-regime slippage model). `ApprovedAction.legs_to_close` (`src/strategy/protocol.py`) is `list[LegClose]`, not `list[str]` (PG-4a, 2026-07-27) — `LegClose` is a frozen dataclass (`leg_role: str`, `instrument_key: str | None = None`); `PaperExecutor.apply()`'s close loop calls `get_position(strategy_name, leg.leg_role, instrument_key=leg.instrument_key)`, threading the resolved instrument through to PG-2a's exact-match path instead of relying on its logged-ambiguity fallback. All 7 concrete strategies (`CSPNiftyV1`, `CCOverlayV1`, `PPOverlayV1`, `CollarOverlayV1`, `IronCondorV1`, `IronCondorV2`, `NiftyTrackComparisonV1`) now populate `instrument_key` on `LegClose` from the already-resolved `PaperPosition` in their `apply_action`/close paths (PG-4b–h), eliminating the roll-overlap wrong-leg risk for those call sites; `StrategyMonitor._dispatch_signal`'s generic auto-execute construction path still emits `LegClose(leg_role=r)` with no `instrument_key` (falls back to PG-2a's WARNING-logged behavior). Concrete strategies: `CSPNiftyV1` (`csp_nifty_v1.py`), `IronCondorV1` (`ic_nifty_v1.py`), `NiftyTrackComparisonV1` (`nifty_track_comparison_v1.py`). All implement `PaperStrategy` protocol. `ICExpiryConfig` (`ic_expiry_config.py`): frozen dataclass with per-expiry entry/exit thresholds (delta bounds, wing width, DTE window, profit target, stop-loss multiplier); `CONFIGS` dict provides four presets — `weekly` (DTE≤14, Tuesday bucket), `monthly` (DTE 15–45), `leaps` (DTE 46–200), `yearly` (DTE 201–420); `STRATEGY_IC_WEEKLY/MONTHLY/LEAPS/YEARLY` constants in `src/paper/constants.py`. `IronCondorV1` constructor accepts `ICExpiryConfig`; `strategy_name` is a property (not class attr); `auto_execute=True`; all thresholds sourced from config; `_auto_select_action()` encodes priority (CLOSE_FULL > spread closes). **Uniform terminal time-stop (2026-08-05, council ruling `docs/council/2026-08-05_ic-time-stop-dte-tiering.md`):** `CONFIGS`' per-bucket `time_stop_dte`/`dte_warn` no longer scale to each bucket's entry-DTE window — monthly/leaps/yearly all use `time_stop_dte=7`, `dte_warn=14` (was 14/21, 45/60, 60/90 respectively); `weekly` unchanged (`time_stop_dte=2`, `dte_warn=4`, its 5–8 DTE entry window makes 7 DTE unreachable). Entry-window bounds (`dte_warn_lo`/`dte_warn_hi`) and the four presets' DTE bands above are untouched — out of scope for this ruling. Phase 0 research default, not a final calibration; paired with counterfactual DTE logging (see `paper_exit_events.counterfactual_dte_marks` below) and a mandatory review after 6 monthly cycles (~2027-02). See DECISIONS.md 2026-08-05. `IronCondorV2ExpiryConfig` (`ic_expiry_config_v2.py`): delta-based config for V2; replaces V1's fixed `wing_width_points` with 10Δ long-wing placement and premium/liquidity floors; `CONFIGS_V2` dict (Phase 1: monthly only); council ruling docs/archive/council/strategy/2026-06-26_ic-v2-core-design.md Stage 3 (D1/D2/D3/D4 decisions). `IronCondorV2` (`ic_nifty_v2.py`): separate strategy class from V1; 25Δ/22Δ entry (D1), 10Δ wings with floors (D2), partial vertical roll adjustment (D3), DTE-tiered exits (D4); `strategy_name` property derives from config.expiry_type; auto_execute=True; 69 KB implementation. `_parse_expiry` (2026-07-23, BUG-018): regex-first (`_EXPIRY_RE`)/BOD-fallback, mirroring `_find_leg`'s BUG-012 pattern — real numeric `instrument_key`s (`NSE_FO|63930`) never matched the old regex-only version, so `check_signals` silently returned `[]` at the expiry gate on every tick since entry, meaning DTE/profit-target/profit-lock/delta-forced-close never evaluated for `paper_ic_nifty_v2_monthly`. Three temporary diagnostic `log.debug` lines added in `check_signals` (remove after 2026-07-24 verification). Tests added but **not run this session** — sandbox disk quota exhausted; needs a live-host `pytest` run to confirm green. `ProfitLockEngine` (`profit_lock_engine.py`): stateless 3-zone evaluator for IC V2 profit-lock decisions; `ProfitLockState` + `ProfitLockDecision` frozen dataclasses; floor formula `max(W,W)+D_cum+D_lock+K ≤ 0.75×C₀` enforced before execution; Zone 1 log-only (25% captured), Zone 2 rolls long wings inward to ~19Δ (50% captured), Zone 3 CLOSE_FULL (75%); auto_execute=True; council ruling docs/archive/council/strategy/2026-06-27_ic-v2-profit-lock-adjustment.md Stage 3. `IronCondorV2ExpiryConfig` updated with `profit_lock: ProfitLockConfig` field; `ProfitLockConfig` frozen dataclass holds zone thresholds, wing delta range (16–22Δ), floor budget 0.75, cost buffer K=10 pts, DTE window 10–22, VIX/IVR guards. **BUG-020 Phase 3, 2026-08-04:** `check_signals`'s shared PnL-computation block (feeding both the profit-target branch and `_check_profit_lock`) now reads `PaperStore.get_original_entry_credit(strategy_name)` (Phase 1, `paper_strategies.original_entry_credit`; Phase 2 populates it from `paper_ic_entry_v2.py::run()`) and substitutes it for the recomputed `entry_credit` when present — anchors captured-fraction to the atomic 4-leg entry credit instead of re-scoping to the surviving legs' credit after a partial close. `None` (pre-Phase-2 positions) or a non-fatal store-read exception (wrapped, logged) falls back to the old recompute unchanged. See DECISIONS.md 2026-08-04 BUG-020 Phase 3. `ExitSignalEngine` (`exit_signals.py`): static rule engine for CSP, CC, PP, and Collar overlay rule sets; returns `ExitSignalResult` with severity (INFO/WARN/ACTION). CSP uses 5 independent classmethods (CR1b): `evaluate_profit_target_csp` (LTP ≤ 30% of entry credit = 70% captured), `evaluate_hard_stop_csp` (LTP ≥ 2× entry), `evaluate_delta_breach_csp` (|δ| ≥ 0.40; state-aware: OPEN→DELTA_BREACH, DEFENDED→DELTA_BREACH_FINAL), `evaluate_time_stop_csp` (days_held ≥ 21), `evaluate_roll_eligible_csp` (DTE ≤ 7). `evaluate_cc`: TIME_STOP added, `DTE_REVIEW` replaces `DTE_FORCED`, `_PROFIT_TARGET_RETENTION = Decimal("0.30")` shared with CSP, `_CC_MIN_ENTRY_CREDIT = Decimal("15")` guards PROFIT_TARGET floor. `evaluate_pp`: no bid/ask params; CRASH_MONETIZE (δ ≤ −0.80 OR value ≥ 5× debit, no spread guard); ROLL_ELIGIBLE ACTION at DTE ≤ 5. `evaluate_roll_overlay(leg_role, dte, base_dte, atm_strike)`: ROLL_ELIGIBLE ACTION at DTE ≤ 5 (when base_dte > 10); ROLL_BASE_FIRST WARN when base_dte ≤ 10. `evaluate_proxy_delta(delta, current_mark, dte, consecutive_days)`: three signals for `base_ditm_call` legs — PROXY_DELTA_CRITICAL ACTION (δ<0.40 for 3 consecutive days), PROXY_PREMIUM_DECAY ACTION (mark<₹0.50 with DTE≥5), PROXY_DELTA_WARN WARN (δ<0.65; suppressed when CRITICAL fires). `CSPNiftyV1` (`csp_nifty_v1.py`): always-open design, `auto_execute=True`, full state machine (OPEN/DEFENDED/RE_ENTRY_PENDING via `TradeState`), three action types (CLOSE_AND_ROLL, ROLL_DOWN_AND_OUT, CLOSE_AND_WAIT). Inherits `ReEntryMixin`; re-entry check on PROFIT_TARGET and TIME_STOP exits. `CCOverlayV1` (`cc_overlay_v1.py`): `auto_execute=True`; single action type (CLOSE_CC); inherits `ReEntryMixin`; re-entry check on PROFIT_TARGET and TIME_STOP exits; Telegram notification after close. `PPOverlayV1` (`pp_overlay_v1.py`): `auto_execute=True`; two-state machine (OPEN / RE_ENTRY_PENDING); three action types (MONETIZE_PP, ROLL_PP, OPEN_NEW_PP); IVR ≤ 0.60 re-entry gate (inverted vs CSP/CC — don't buy at peak post-crash IV). Does NOT inherit `ReEntryMixin` (inverted gate requires custom `_evaluate_pp_reentry`). `CollarOverlayV1` (`collar_overlay_v1.py`): `auto_execute=True`; inherits `ReEntryMixin`; atomic close via `OverlayCloser.close_collar_all`. **Collar3b, 2026-08-04:** unified atomic exit+immediate-reenter — `apply_action` now accepts `CLOSE_AND_REENTER_COLLAR` (legacy `CLOSE_COLLAR` unchanged) driven by 5 priority-ordered signals via new `_select_combined_reentry_action` (mirrors `IronCondorV1._auto_select_action`): `CRASH_MONETIZE` (put, net-new) > `LOSS_STOP` > `PROFIT_TARGET` > `DTE_REVIEW` (DTE≤5, `TIME_STOP` deliberately excluded) > `DELTA_STOP`. On the winning signal, both legs close atomically then `_reenter_collar` immediately reselects+reopens a fresh pair via new `src/strategy/collar_entry.py::select_and_build_collar_entry()` (reads only the live chain via a `broker` constructor param, `None` → reentry skipped with WARNING, e.g. in tests). Reentry selection failure (`CollarEntrySelectionError` or unexpected exception) logs ERROR, sends a Telegram alert instructing manual entry, and leaves the position flat — no auto-retry. `src/strategy/exit_signals.py` gained `ExitSignalEngine.evaluate_crash_monetize()`, extracted from `evaluate_pp`'s existing δ≤-0.80/value≥5x logic (behavior unchanged) so Collar's put leg can reuse it directly. `--auto-collar` + `auto_collar_bootstrap()` added to `paper_3track_overlay_entry.py` for first-ever bootstrap only (routine reentry needs no cron, it's event-triggered) — hard-blocked to `--dry-run` only, not yet proven live. See DECISIONS.md 2026-08-04 Collar3b, `docs/plan/3track-consolidation/stories.md` Collar3b. **Collar3a, 2026-08-03:** `apply_action`'s re-entry trigger guard widened from `("PROFIT_TARGET", "TIME_STOP", "DTE_REVIEW")` to also include `LOSS_STOP`/`DELTA_STOP` (both ACTION-severity, both route through `CLOSE_COLLAR` via `evaluate_cc()` but previously skipped `_check_reentry`) — mirrors CC3's exact shipped trigger set for `CCOverlayV1`; `BELOW_FLOOR` deliberately excluded (INFO-severity, never dispatches `CLOSE_COLLAR`). Only the trigger tuple changed — gates and atomic close untouched. See DECISIONS.md 2026-08-03 Collar3a, `docs/plan/3track-consolidation/stories.md` Collar3a. `NiftyTrackComparisonV1` (`nifty_track_comparison_v1.py`): **`auto_execute=True` (S4, 2026-07-29)** — ROLL_ELIGIBLE (DTE ≤ 5), ROLL_DUE_DTE (DTE 6–10, target available), and ROLL_DUE_DECAY (target available) all now resolve a roll target via `_select_overlay_roll_target` and set `auto_execute`/`auto_action=ROLL_OVERLAY`/`legs_to_open`/`mark` in the payload so `StrategyMonitor._route_event` dispatches straight to `apply_action` with no Telegram approval step; without a resolvable target, the event carries no `auto_execute` key and still routes to approval. Proxy-delta `RECORD_REENTRY` ACTION events (base_ditm_call) are deliberately excluded — `RECORD_REENTRY` is not in `_ALLOWED_ACTIONS`, so this signal always stays on the manual approval path. `apply_action` now persists the roll: a new `_persist_roll()` helper writes the close leg (price from `action.metadata["mark"]`, falling back to `avg_sell_price`/`avg_cost`) and the open leg (price from `LegSpec.price`, captured at selection time in `_select_overlay_roll_target`) as a single atomic `store.record_trades()` call — this used to be in-memory-only, silently relying on an "executor" dispatch step that never existed anywhere in the codebase (the same failure class as the 2026-07-15 IC incident: compute a roll, never write it). Any leg whose price can't be resolved (or a flat `net_qty == 0` closed position) is skipped with a WARNING log rather than persisted with a fabricated price. Wires `evaluate_proxy_delta` for `base_ditm_call` legs via `PaperStore.get/set_proxy_delta_breach_count`. `src/strategy/monitor.py::StrategyMonitor._route_event`'s generic auto-execute dispatch was fixed alongside this (previously hardcoded `legs_to_open=[]` on every auto-execute `ApprovedAction`, which would have made this strategy's ROLL_OVERLAY raise `ValueError` inside `apply_action` and get silently swallowed by `_route_event`'s bare `except Exception`) — now threads `event.payload.get("legs_to_open", [])` through; close-only strategies (CC/PP/Collar) are unaffected since their payloads never set that key. `src/strategy/protocol.py::LegSpec` gained an optional `price: Decimal | None = None` field (appended after `notes`, purely additive — no existing call site broken) so a roll target's live LTP at selection time survives through to the persistence step. **Track-ownership overlay blocks removed (S2r, 2026-07-29, SHA abdb7ef):** `_check_futures_cc_block` (formerly emitted BLOCKED_COMBINATION on standalone short call for `paper_nifty_futures`) and a second, undocumented futures+`overlay_cc` hard-block inside `_select_overlay_roll_target` are both deleted — overlay entry/roll is track-independent by operator decision (DECISIONS.md 2026-07-29 round 5), never gated on which track's context it is evaluated from. `_select_overlay_roll_target` no longer takes a `strategy_name` param. All ACTION-severity `SignalEvent` payloads include `"valid_actions": [...]`, used by `TelegramGateway.send_approval_request` to build the approval keyboard without a council call. `OverlayCloser` (`overlay_closer.py`): atomic multi-leg close orchestrator; `close_collar_all` and `monetize_collar_put` use `store.record_trades([...])` for single DB transaction; `monetize_collar_put` validates put leg exists before touching call (incomplete-collar guard). `close_collar_all` returns `bool` (2026-07-22 fix): `True` if the position ends up flat (already-flat, or the write succeeds), `False` if `record_trades` fails and both legs are still open — `auto_close_overlay`'s `overlay_collar_call` branch checks this and raises into its own failure-notification path on `False`, so a write failure can no longer be reported as a successful "COLLAR CLOSED" with a plausible-looking P&L (see DECISIONS.md 2026-07-22). `auto_close.py` (AUTO-1): EOD auto-close execution via `OverlayCloser` for all overlays, status resolution to ACTED, unified Telegram close notification, EOD PP re-entry eligibility checks (`evaluate_pp_reentry_eod`). `close_ic_legs()` (`ic_close_executor.py`, 2026-07-15, settlement-fallback revised 2026-07-16): shared auto-close persistence helper for `IronCondorV1`/`IronCondorV2` — batch-fetches live LTP via `broker.get_ltp()`; writes opposite-action closing `PaperTrade` rows atomically via `store.record_trades()`. Called from both classes' `apply_action()` for `CLOSE_FULL`/`CLOSE_CALL_SPREAD`/`CLOSE_PUT_SPREAD` when auto-executing (fixes a bug where those actions filtered the in-memory position list but never wrote the close to `paper_trades`, causing the triggering exit signal to silently re-fire every tick indefinitely — see DECISIONS.md 2026-07-15). When a leg's LTP is missing, fallback now branches on cause: if `InstrumentLookup.from_file(DEFAULT_BOD_PATH)` shows the leg's expiry is today or earlier, price is derived from live Nifty spot (`NSE_INDEX|Nifty 50`) — intrinsic value `|spot−strike|` for ITM legs, fixed `Decimal("0.05")` tick floor for OTM legs (ATM falls into the OTM branch, intrinsic there is 0 either way). Entry-price fallback (`avg_sell_price`/`avg_cost`) is reserved for transient LTP gaps on still-live contracts or when BOD/spot resolution itself fails. Known approximation: live spot at detection-tick, not NSE's actual final settlement price (VWAP 3:00–3:30 PM expiry day) — see DECISIONS.md 2026-07-16. `ROLL_WING`/`PROFIT_LOCK_ZONE2` have the same gap on their close side, deferred as IC-CLOSE-2 (TODOS.md) pending replacement-leg strike-selection logic. `_send_close_notification` (both `IronCondorV1`/`IronCondorV2`, 2026-07-24): Telegram close-confirmation message now includes a `Net P&L` line via `get_strategy_realized_pnl()` (`src/paper/tracker.py`), computed with a deferred (function-local) import to avoid a circular import through `src.paper.store` → `src.strategy.profit_lock_engine`. **BUG-022, 2026-08-04:** `src/strategy/roll_utils.py` gained `evaluate_floor_formula` (extracted from `ProfitLockEngine._evaluate_floor_formula` — same inequality, `max(W_put,W_call)+D_cum+D_lock+K ≤ floor_budget×C0`) and `search_narrow_wing_replacement` (exhaustive widest-to-narrowest strike walk between a threatened short and its current wing, both endpoints excluded, gated by liquidity/premium floors + the inequality). Both `IronCondorV2._execute_partial_roll` (Guard 3) and `IronCondorV1.check_signals`'s delta-stop block now fall back to this shared search when their original single-candidate wing lookup fails, before conceding; on exhaustion (or any other roll-guard failure) both strategies escalate `DELTA_STOP` unconditionally to `CLOSE_FULL` — the naked single-side `CLOSE_CALL_SPREAD`/`CLOSE_PUT_SPREAD` outcome no longer exists. V1 gains a liquidity/premium floor on its wing search for the first time (previously had none) via a module-level `_WING_SEARCH_FLOOR_DEFAULTS = ProfitLockConfig()` (V1 has no floor config of its own). See DECISIONS.md 2026-08-04 BUG-022.
`src/portfolio/overlay_coverage.py` — `compute_overlay_coverage(store, broker, lookup, track_namespace, snapshot_date) -> OverlayCoverage` (3track-consolidation S3r, 2026-07-29) — query-time-only overlay coverage ratio per 3-track base (Spot/Futures/Proxy), never persisted, never duplicated per track. Pulls the track's own base leg position plus the shared track-independent overlay namespace (`STRATEGY_OVERLAY = "paper_nifty_overlay"`, `src/paper/constants.py`, S1r) and computes `overlay_effective_units / track_effective_units * 100`; `coverage_pct=None` when the track has no open base position (undefined, not zero); can be negative for a directionally-correlated (non-hedging) overlay leg. Futures notional is `qty * 1.0` (linear future delta), not `paper_margin_snapshots` — that table is IC-only (`capture_entry_margin()` is never called from the 3-track path; a graph edge suggesting otherwise was a false positive). Shares Greeks resolution with `generate_track_snapshot` via the new `resolve_leg_delta()` helper (`src/paper/track_snapshot.py`, extracted from `generate_track_snapshot`'s former inline loop body — behavior-preserving, see DECISIONS.md 2026-07-29 S3r). `OverlayCoverage` frozen dataclass in `src/paper/models.py`. 5 tests in `tests/unit/portfolio/test_overlay_coverage.py`. `TrackPnL` (`src/paper/track_snapshot.py`) gained a `raw_overlay_pnls` field (S7, 2026-08-01, SHA 192be41) — real leg_role keys (`overlay_cc`/`overlay_pp`/`overlay_collar_call`/`overlay_collar_put`) captured before `_normalize_overlay_pnls()` collapses them to display labels (`"cc"`/`"pp"`/`"collar"`); `_save_leg_snapshots()` (`scripts/strategies/three_track/paper_3track_snapshot.py`) now persists overlay legs off `raw_overlay_pnls`, not the collapsed dict — fixes a bug where `overlay_ltp` was always `None` on every CC/PP/Collar `paper_leg_snapshots` row because `get_position()` was called with a non-existent leg_role. See DECISIONS.md 2026-08-01 S7. **`OverlayPnLSnapshot`** frozen dataclass (`src/paper/models.py`, S8, 2026-08-01) — daily per-overlay P&L row mirroring S3's `TrackComparisonSnapshot` shape (`pnl_1d_abs/pct`, `pnl_inception_abs/pct`) but keyed `(strategy_name, overlay_type, snapshot_date)` with `overlay_type ∈ {"cc","pp","collar"}` instead of per-base-track. Persisted in `paper_overlay_pnl_snapshots` (STRICT, TEXT-stored Decimals) via `PaperStore.record_overlay_pnl_snapshot()` (upsert) / `get_overlay_pnl_snapshots(strategy_name, overlay_type, start_date=None, end_date=None)`. Computed in `scripts/strategies/three_track/paper_3track_snapshot.py` by `_compute_overlay_pnl_snapshots()`, reading only the real-leg-role `paper_leg_snapshots` rows S7 produces (never the collapsed display dict); `_overlay_type_groups()` mirrors `_normalize_overlay_pnls()`'s precedence exactly (collar_call takes precedence over standalone cc; collar_call+collar_put merge into one "collar" row; a lone collar_put with the call already closed/rolled off still reports as "collar", logged via `overlay_pnl.collar_put_without_call` WARNING rather than silently dropped — a real lifecycle state, not a bug). `_leg_entry_basis()` picks `avg_sell_price` for short/credit legs (net_qty < 0, e.g. CC's sold call) and `avg_cost` for long/debit legs (e.g. PP's bought put) — `PaperPosition.avg_cost` is BUY-only and would silently zero a short leg's denominator otherwise. No overlay-specific P&L sign inversion (confirmed with operator, 2026-08-01) — `pnl_abs` is already direction-aware via mark-to-market, so CC/PP/Collar all use the same `pnl_inception_pct = pnl_inception_abs / abs(entry_basis)` formula S3 uses for base legs. Called from `_run()` only when `save=True`, right after `_save_leg_snapshots`/S3's comparison snapshot. 11 tests in `tests/unit/scripts/test_paper_3track_overlay_pnl.py` + 6 in `tests/unit/paper/test_store.py`. See DECISIONS.md 2026-08-01 S8, `docs/plan/3track-consolidation/stories.md` S8. **`ProtectionRecoverySnapshot`** frozen dataclass (`src/paper/models.py`, S9, 2026-08-01) — one row per `snapshot_date` in `paper_protection_recovery_snapshots`, combining S3's NiftyBees (`STRATEGY_SPOT`) row with S8's cc/pp/collar rows for the same date — no independent leg-level computation, per operator confirmation (2026-08-01) that CC/PP/Collar run as three live parallel overlay positions on NiftyBees (not one-live/two-hypothetical). `recovery_pct`/`best_overlay` (and the separate `..._inception` pair) are `None` whenever the relevant NiftyBees P&L is `>= 0` — a green/flat day has nothing to recover, never a negative or zero-anchored figure; inception recovery is computed independently of the daily pair, not a running sum. Persisted via `PaperStore.record_protection_recovery_snapshot()` (upsert) / `get_protection_recovery_snapshots(start_date=None, end_date=None)` — **deviates from the story spec's signature by dropping `strategy_name`**: the table has no such column (it's a single NiftyBees-anchored series, not per-strategy), so the param would have been dead weight. Computed in `scripts/strategies/three_track/paper_3track_snapshot.py` by `_compute_protection_recovery_snapshot()`, called from `_run()` right after S3's synthetic spot row is persisted (`save=True` only). `_build_recovery_digest()` builds a single compact Telegram message — overlay lines sorted by recovery amount descending on a red day (with a "Best:" line) or raw P&L descending on a green day (recovery framing dropped entirely, no "Best:" line, no percentages); one `notifier.send()` call per cron run, never per-overlay; suppressed in dry-run. 12 tests in `tests/unit/scripts/test_paper_3track_protection_recovery.py` + 6 in `tests/unit/paper/test_store.py`. See DECISIONS.md 2026-08-01 S9, `docs/plan/3track-consolidation/stories.md` S9.

**BUG-028 Phase 1, 2026-08-10 (council-ruled "B-lite" decoupling):** overlay P&L (CC/PP/Collar) is now computed and persisted entirely independent of the 3-track base strategies. `TrackPnL` (`src/paper/track_snapshot.py`) is base-leg-only again — `overlay_pnls`/`raw_overlay_pnls` fields removed; `generate_track_snapshot()` no longer discovers/attributes `overlay_*`-role legs at all (a leftover pre-S2r overlay row under a track's own strategy_name is now silently skipped, not double-reported — Phase 3's historical repair script reattributes those rows). `_normalize_overlay_pnls()` deleted (dead). New standalone pipeline in `scripts/strategies/three_track/paper_3track_snapshot.py`: `_compute_overlay_leg_totals()` (async — fetches LTP, computes per-leg-role unrealized/realized/total P&L directly from `STRATEGY_OVERLAY`'s own trades/positions, in-memory), `_save_overlay_leg_snapshots()` (persists to `paper_leg_snapshots` under `strategy_name=STRATEGY_OVERLAY`), `_overlay_summary_row()` (one standalone "Overlay (standalone)" row in the printed comparison table, independent of `--tracks` selection; returns `None` — row omitted — when the overlay book has never been entered, never a false `₹0`). `_compute_overlay_pnl_snapshots()` reworked to query `STRATEGY_OVERLAY` directly (was inheriting whichever track's strategy_name called it — the actual root cause of the bug: real overlay rows have been filed under `STRATEGY_OVERLAY` since S2r, 2026-07-29, but this function never looked there). `_compute_protection_recovery_snapshot()`'s overlay-side read switched from `STRATEGY_SPOT` to `STRATEGY_OVERLAY` (NiftyBees comparison itself stays anchored on `STRATEGY_SPOT`, untouched). `_save_leg_snapshots()` (base-track leg persistence) no longer writes overlay rows at all. Dead code removed: `_overlay_roles_for_track`, `_print_summary_table` (zero callers). `scripts/dev/paper_track_snapshot.py` (legacy backward-compat script) trimmed to base-leg-only display, same reason. Phase 3 (historical repair of pre-fix `paper_overlay_pnl_snapshots` rows) not yet implemented — see `docs/bugs/task.md` B028.11+. See `docs/council/2026-08-10_overlay-pnl-reporting-track-independence.md`, DECISIONS.md 2026-08-10, `docs/bugs/bugs.md` BUG-028.

**BUG-028 Phase 2, 2026-08-10 (eliminate silent false zeros):** `ProtectionRecoverySnapshot`'s `cc_pnl_1d`/`pp_pnl_1d`/`collar_pnl_1d` + `_inception` counterparts (`src/paper/models.py`) are now `Decimal | None` — `None` means "no `OverlayPnLSnapshot` row for that type/date," distinct from a genuine `Decimal("0")`. `paper_protection_recovery_snapshots` was a `STRICT` table with those 6 columns `TEXT NOT NULL`; `PaperStore.__init__` gained a one-time rebuild migration (create-new/copy/drop/rename, same pattern as the `paper_trades` UNIQUE-constraint migration) detected via `PRAGMA table_info`'s `notnull` flag on `cc_pnl_1d` (not a string match against `sqlite_master.sql`). `_compute_protection_recovery_snapshot()` defaults `overlay_1d`/`overlay_inception` to `None` per type and logs `protection_recovery.overlay_source_missing` (strategy/overlay_type/date) when `get_overlay_pnl_snapshots` returns no rows. `_best_recovery()` skips `None` entries — a missing overlay can't be "best." `_build_recovery_digest()` renders `"  {label:<6} No data"` for `None` fields (sorted after real-valued lines), unchanged `+0`-style rendering for a genuine zero. See `docs/bugs/task.md` B028.8–B028.10.

**BUG-028 Phase 3, 2026-08-10 (historical repair, one-off script):** `scripts/dev/migrate_overlay_pnl_attribution.py` — mirrors `migrate_paper_trades_state.py`/`backfill_nav_total_pnl.py`'s pattern (`--db-path`/`--dry-run`, `_SCRIPT_NAME` structlog logger). Backs up the DB before any write; derives the S2r cutover date from `MIN(paper_trades.trade_date)` where `strategy_name = STRATEGY_OVERLAY` (not a hardcoded commit date); for every pre-cutover `paper_overlay_pnl_snapshots` row filed under a legacy 3-track `strategy_name` (`STRATEGY_SPOT`/`STRATEGY_FUTURES`/`STRATEGY_PROXY`), checks `(STRATEGY_OVERLAY, overlay_type, snapshot_date)` for a collision before relabeling — a collision is skipped with a logged WARNING (`migrate_overlay_pnl_attribution.collision_skipped`) and the legacy row is left intact, never blind-overwritten. Relabel is a single `UPDATE strategy_name` (never an INSERT), so no dual-write is structurally possible. Returns a `MigrationResult(migrated, skipped, unchanged)` — `unchanged` is always 0 by construction (every candidate row is either migrated or skipped) but kept for output-contract parity with B028.11's spec. 5 tests in `tests/unit/scripts/test_migrate_overlay_pnl_attribution.py` — **not executed this session** (sandbox has no free disk for `pytest`/deps); needs a live-host confirmation run before being trusted as green. See `docs/bugs/task.md` B028.11–B028.13.

**BUG-029, 2026-08-10 (discovered):** `paper_exit_events.counterfactual_dte_marks` — the column added by commit `17b4ff9` (2026-08-05) to `_SCHEMA`/`create_exit_event`/`get_exit_event`/`get_open_exit_events` — was never actually migrated onto the live DB, even though a migration script (`scripts/dev/migrate_exit_events_counterfactual_dte_marks.py`) was committed the same day. Result: `scripts/strategies/three_track/paper_3track_snapshot.py`'s `35 15 * * 1-5` EOD cron has crashed at `compute_and_record_exit_signals()` → `store.get_open_exit_events()` (`sqlite3.OperationalError: no such column`) every market day from 2026-08-05 through 2026-08-10 — before reaching exit-signal evaluation or BUG-028's overlay P&L/leg-snapshot/protection-recovery writes. This session added test coverage for the pre-existing migration script (`tests/unit/scripts/test_migrate_exit_events_counterfactual_dte_marks.py`, 4 tests — the script itself had none before) and logged the bug; **running the migration against the live DB is still outstanding**, left for a live-host session. See `docs/bugs/bugs.md`/`docs/bugs/task.md` BUG-029.

`src/council/` — AI council infrastructure. `RapidCouncil` (`rapid.py`): parallel Stage-1 fan-out (5 heterogeneous personas) + chairman synthesis + timeout handling.
Scripts (under `scripts/` structured into functional axis):
   - `pipeline/`: `upstox_chain_snapshot.py` (EOD option chain → Parquet), `upstox_chain_intraday.py` (5-min intraday chain → Parquet), `gamma_daily_watch.py` (Greeks monitoring), `bhavcopy_bootstrap.py` (resumable bulk NSE bhavcopy download 2016–present).
   - `lookup/`: `find_strike_by_delta.py` (live chain → filter by delta range; enforces `_apply_liquidity_gate()` — rejects strikes with insufficient OI/volume; exits with error if no candidate passes; auto-select fallback ladder is now side-aware via `_select_delta_candidates(option_type)` — `--option-type CE` uses `CC_DELTA_CANDIDATES = [0.18, 0.20, 0.15]` (CC2-confirmed, no longer provisional, see DECISIONS.md 2026-08-01), everything else (`PE`/`BOTH`) keeps the original CSP-targeted `DELTA_CANDIDATES = [0.22, 0.25, 0.20]` unchanged — previously CC silently inherited CSP's ladder regardless of `--option-type`, see 3track-consolidation CC1; **CC4, 2026-08-01:** `_reorder_cc_round500_first()` — CC-only (`--option-type CE`), applied after `rank_strikes`/`_apply_liquidity_gate` on the already-gated candidate set, reorders round-₹500 strikes (24500, 25000, …) ahead of round-100 strikes without excluding the latter; cushion bar is the existing liquidity gate itself (no new OI/spread comparison, operator-resolved); falls back to the best round-100 candidate (existing order preserved) with a logged reason when no round-500 candidate clears the gate; does not touch shared `rank_strikes()`, so CSP/IC/PP paths are unaffected; cross-expiry reach (round-500 on quarterly/yearly beating round-100 on the nearer expiry) is possible and intentional — this was already inherent in the pre-existing candidate loop, which pools rows across all resolved expiries before ranking/gating); **PP1, 2026-08-03:** `_select_delta_candidates(option_type, overlay_type=None)` gained an `overlay_type` param and new `PP_DELTA_CANDIDATES = [0.20, 0.25, 0.15]` (PROVISIONAL — experimentation/comparison tool only, gated live by the separate PP2 decision-gate story) — returned only when `option_type == "PE"` **and** the new explicit `--overlay-type pp` CLI flag is passed; bare `--option-type PE` (CSP's existing invocation shape) is untouched and still resolves to `DELTA_CANDIDATES`, by design — PE alone is ambiguous between CSP's short-put and PP's long-put use of this script, so the PP ladder is opt-in only, never inferred (operator-scoped deferral: the collision only matters once CSP and PP are both live simultaneously, not the case yet). `--overlay-type cc` is a documented no-op (CE already resolves to `CC_DELTA_CANDIDATES` on its own, checked first). Confirmed (not assumed) that `rank_strikes()` needs no PP-specific ranking tuple — already documented side-agnostic. `find_overlay_strikes.py` (overlay-specific strike finder), `instrument_lookup.py` (BOD JSON key resolver). **PP3, 2026-08-03:** `paper_3track_overlay_entry.py` gained `auto_pp_bootstrap()` (`--auto-pp` flag), mirroring CC3's `auto_cc_bootstrap()` shape — daily cadence (not CC3's weekly Wednesday), two entry triggers via new `_open_pp_dte()` helper: no open `overlay_pp` position (bootstrap) or DTE ≤ 5 on the open one (routine roll, matches `evaluate_pp`'s `ROLL_ELIGIBLE` threshold via `_PP_ROLL_DTE_THRESHOLD`); DTE > 5 is a no-op (`sys.exit(0)`). The routine-roll trigger deliberately holds two puts briefly (fresh one bought same-day, before the outgoing DTE≤5 put's own `ROLL_PP` close necessarily lands) to satisfy "no unprotected day" — the generic S6 one-time-bootstrap gate (`_has_open_overlay_leg`) is bypassed specifically on the `--auto-pp` path since it would otherwise incorrectly re-block that overlap. IVR re-entry gate reuses IC's `resolve_ivr`/`GateViolation` log-only-gates pattern (`--log-only-gates`, default on) instead of an unconditional bypass — a below/above-threshold IVR persists `GateViolation(gate_name="ivr_pp_reentry", strategy_name=STRATEGY_PP_OVERLAY, ...)` via `PaperStore.record_gate_violation` and entry proceeds; structural gates (BOD load, DTE<14, chain fetch, no eligible strike) always hard-abort. Investigated (not fixed, confirmed correct-as-is) whether `PPOverlayV1.apply_action`'s `ROLL_PP` branch should call `_check_reentry` like `MONETIZE_PP` does — it should not: the mixin's DTE≥14 gate would evaluate against the just-closed ≤5-DTE contract and always report BLOCKED, since ROLL_PP is contract continuation, not a fresh-cycle exit. See DECISIONS.md 2026-08-03 PP3 entry, `docs/plan/3track-consolidation/stories.md` PP3. 9 new tests in `tests/unit/paper/test_overlay_entry.py`, 60/60 confirmed green on a live `pytest` run (worked around the sandbox's full `/sessions` disk via `pip install --target=/tmp/...`). **Collar1, 2026-08-03:** `find_strike_by_delta.py` gained `--overlay-type collar` — coordinates CC1's `CC_DELTA_CANDIDATES` (short call) and PP1's `PP_DELTA_CANDIDATES` (long put) into a two-leg search via new `_find_candidates_for_ladder()` (collects one liquidity-gated candidate per ladder rung, unlike `main()`'s single-winner fallback loop), `compute_net_collar_premium()` (call credit − put debit, mid-price convention matching `build_record_command`), `build_collar_cross_product()`, `format_collar_table()`, and `run_collar_mode()` (raises `RuntimeError` if either ladder is missing/empty — hard-dependency guard, not cosmetic). Deliberately does **not** auto-select a single combo — prints the full call×put cross-product with net premium, leaving the pick to Collar2. `main()` short-circuits to this mode before the single-selection fallback loop when `--overlay-type collar` is passed; `--option-type` is ignored in this mode. 10 new tests in `tests/unit/test_find_strike_by_delta.py` (56/56 total confirmed green; sandbox `/sessions` disk was again at 100%, worked around via `pip install --target=.../mnt/outputs/pydeps`, same class of fix as PP1/PP3/CC3). Reviewed via `general-purpose` agent standing in for `@code-reviewer` — no CRITICAL/ERROR; two low-risk WARNINGs deferred (wasted-but-discarded fetch loop when `option_type` default runs alongside collar mode; unbounded cross-product table if ladders grow beyond today's 3×3). See `docs/plan/3track-consolidation/stories.md` Collar1.
   - `record/`: `record_paper_trade.py` (entry/close for paper strategy; enforces liquidity gate + R3 IVR hard block on SELL; `--force-entry` flag overrides low-IVR block with logged warning), `record_trade.py` (live trade recording).
   - `strategies/`: strategy-specific scripts (e.g. `three_track/paper_3track_snapshot.py` EOD cron — includes Tier 1 exit signal detection via `ExitSignalEngine` for all leg roles (CSP/CC/PP/Collar), deduplication against existing OPEN events, and `_check_base_expiry()` for DTE ≤ 5 base-position expiry detection with roll alert via Telegram — `base_ditm_call` rolls via `InstrumentLookup.get_next_contract_in_band()` (band-aware: monthly/quarterly/yearly, skips intervening weeklies, fixed 2026-07-20 — see DECISIONS.md), `base_futures` still via `get_next_contract()` (safe: NIFTY futures are monthly-only, no weekly contract exists); CSP and 3-track overlay rolls are now backbone-managed (`CSPNiftyV1.apply_action` + `NiftyTrackComparisonV1.apply_action` via `PaperExecutor`; retired scripts: `csp/paper_csp_roll.py`, `three_track/paper_3track_overlay_roll.py` — PA2); `three_track/paper_3track_roll.py` (S5, 2026-07-30) — automated base-leg roll executor for `base_futures` (DTE≤1) and `base_ditm_call` (DTE<20), separate from overlay automation/`NiftyTrackComparisonV1`; resolves the next-band contract via `get_next_contract`/`get_next_contract_in_band`, applies warn-only liquidity gates (futures: relative OI ≥10% of near-month via a best-effort broker OI hook that's currently a documented no-op stub; DITM: reuses `PROXY_OI_MIN`/`PROXY_SPREAD_MAX`), and persists close+open atomically via a single `PaperStore.record_trades()` call, flagging a `partial` roll (ERROR log + distinct Telegram message) if only one leg lands. See DECISIONS.md 2026-07-30 S5 entry. **S6, 2026-07-30 — full unattended automation:** `three_track/paper_3track_entry.py` and `three_track/paper_3track_overlay_entry.py` are now cron-safe (and `paper_3track_entry.py` supports `--auto-futures`/`--auto-ditm` flags — `--confirm` writes live, per-track, once EC-5 landed and was verified, see DECISIONS.md 2026-08-04) — both gate their write path on a one-time-bootstrap check (`_has_open_base_positions()` / `_has_open_overlay_leg()`, keyed on `STRATEGY_SPOT`/`FUTURES`/`PROXY` or `STRATEGY_OVERLAY`'s per-leg marker role respectively) that skips entry entirely once any relevant position is already open — there is no recurring cycle to re-enter, so a scheduled invocation is idempotent by construction rather than by external locking. Both scripts now notify Telegram (`build_notifier()`/`TelegramNotifier.send()`) on a successful bootstrap entry, non-fatal (notify failure logs WARNING, never blocks the trade) — same contract as `paper_3track_roll.py`'s roll notification, which already existed from S5. **CC3, 2026-08-01 (SHA 5d63430, 3175ac3):** `three_track/paper_3track_overlay_entry.py` gained `auto_cc_bootstrap()` (`--auto-cc` flag) — automates CC overlay entry by fetching the live chain, selecting a strike via `_select_delta_candidates`/`CC_DELTA_CANDIDATES` (0.18–0.20 band, reused from CC1, not a hardcoded value) and the same `_apply_liquidity_gate`/`rank_strikes` chain, applying DTE≥14/IVR≥0.25 gates matching `ReEntryMixin` exactly (IVR sourced from `load_vix_series().iloc[-1]`, the same last-ingested-Parquet-point `ReEntryMixin` uses — an initial version wrongly used a live `fetch_vix_latest()` call, causing potential gate-decision divergence, fixed same session), and sending a non-fatal Telegram notification on success. `--no-dry-run` is hard-blocked (`sys.exit(1)`) with no bypass — **stays dry-run-only until CC5/EC-5 (`paper-exit-codification` epic) resolves.** Also widened `CCOverlayV1.apply_action()`'s reentry-check trigger from `("PROFIT_TARGET","TIME_STOP")` to all four ACTION exit signals (adds `LOSS_STOP`, `DELTA_STOP`) and added an idempotency guard (`_query_open_call_role`) preventing duplicate `overlay_cc` entries. See `docs/plan/3track-consolidation/tasks.md` CC3. All three call sites' Telegram messages use plain text, not markdown `*bold*` (fixed in `paper_3track_roll.py` alongside this story — `TelegramNotifier.send()` wrapped in `<pre>` with `parse_mode: HTML` at the time, so markdown asterisks rendered as literal characters, not bold — **superseded 2026-08-25**: `send()` and `TelegramGateway.send_notification`/`send_approval_request` now all send `parse_mode: MarkdownV2` with mandatory caller-side escaping via `escape_markdown()`/`mdcode()` (`src/notifications/markdown.py`) and a static-scan guard test (`tests/unit/notifications/test_escaping_guard.py`); see `docs/plan/telegram-markdown-migration/` MD-1…MD-7.3 and DECISIONS.md 2026-08-25 "Telegram Markdown migration closed"). See DECISIONS.md 2026-07-30 S6 entry; `cc_calibration/paper_cc_entry.py` delta-based CE selection for CC overlay entry (|delta| ∈ [0.12, 0.18], 15Δ target, IVR gate, `compute_max_lots` quantity constraint, dry-run output); `cc_calibration/paper_cc_roll.py` manual override exit for CC overlay with four triggers matching `evaluate_cc()` thresholds: loss_stop 2.5×, delta_stop 0.55, profit_target 30% remaining with entry ≥ ₹15, time_stop 21d; `--dry-run` and `--force` flags; `ic/paper_ic_entry.py` config-driven entry helper for all four V1 IC variants with IVR/duplicate/DTE/liquidity gates; `ic/paper_ic_entry_v2.py` V2-only entry helper — delta-based 10Δ wing placement via chain scan, `long_wing_min_premium` floor enforced, shared gates via `ic_entry_gates.py`; `_post_expiry_gate()` blocks entry only on the same day the most recently settled Nifty monthly expiry falls (via `_most_recently_settled_expiry()`, current month's last Tuesday if already passed else previous month's, year-rollover safe — fixed BUG-003, was wrongly blocking the entire new cycle against the *current* month's own future expiry); DTE window 20–32 (post-expiry cadence); Telegram alert on gate failures (2026-08-11 audit found this was previously true only for V1's partial-execution-failure/success paths and V2's duplicate/post-expiry/ivr/long_wing_floor gates — `resolve_expiry`'s own structural exits (BOD missing/load-failed/no-candidate-found — the exact `auto_pp.no_monthly_expiry_found`-class failure), chain fetch, leg resolution, liquidity hard-blocks, and spot fetch were silent in both scripts; V1 had no gate alerting at all. Fixed: `resolve_expiry()` gained a `notifier` callable param matching `check_duplicate`/`resolve_ivr`'s contract; V2 wired it plus its remaining gaps; V1 gained its own local `_gate_alert` closure mirroring V2's, covering all 16 structural exits — see DECISIONS.md 2026-08-11 "Monthly expiry DTE floor 15→14; gate-failure Telegram alerting audit". Known limitation carried forward: `_gate_alert`'s `asyncio.get_running_loop().create_task(...)` is fire-and-forget, not awaited — a `sys.exit(1)` immediately after can in principle tear down the loop before the send completes; pre-existing in V2, now also present in V1, not fixed this session); `ic/ic_entry_gates.py` shared pre-entry gate helpers (check_duplicate, resolve_ivr, resolve_expiry) used by V2 (V1 duplicates check_duplicate/resolve_ivr/expiry-resolution inline rather than importing these — a pre-existing divergence, not touched this session); `resolve_ivr` now includes `_is_vix_window_stale()` recency guard (BUG-004) — VIX window >7 calendar days behind today is treated as gate-data-unavailable (WARNING logged, `ivr=None`, existing hard-block path) rather than silently computing IVR against a stale window; **portfolio-delta gating/self-adjustment removed from both entry scripts entirely (2026-07-03)** — IC entries are now judged only on their own two short legs' delta, never against other strategies' or other IC variants' open positions (`ic_relevant_strategy_names()` still exists in `ic_entry_gates.py` for its own tests but is no longer called by either entry script); `record_paper_trade.py`'s separate account-wide delta-cap check is now also skipped for any `paper_ic_*` strategy, same rationale — see DECISIONS.md 2026-07-03 "IC entries judged in isolation"; `ic/paper_ic_snapshot.py` EOD audit cron; V1 loop iterates all four IC variants; V2 loop covers `CONFIGS_V2` with `IronCondorV2` strategy class; Telegram report; `ic/paper_ic_monthly_comparison.py` EOD V1 vs V2 monthly comparison cron; `ICMonthlyStats` dataclass; side-by-side Telegram report; cron `45 15 * * 1-5`). **2026-08-07:** `--log-only-gates` (already IC/`--auto-pp`'s pattern) extended to `auto_cc_bootstrap`/`auto_collar_bootstrap` in `paper_3track_overlay_entry.py` — both now take a `log_only_gates: bool = True` kwarg and return `tuple[OverlayConfig | None, GateViolation | None]` (was `OverlayConfig | None`), matching `auto_pp_bootstrap`'s existing shape exactly. A below-0.25 IVR reading now logs a `GateViolation` (`gate_name="ivr_cc_reentry"`/`"ivr_collar_reentry"`, `strategy_name=STRATEGY_CC_OVERLAY`/`STRATEGY_COLLAR_OVERLAY`) and proceeds, instead of hard-blocking — paper-trading phase, no real capital at risk (Animesh, 2026-08-07). Structural gates (BOD load, no monthly expiry, DTE < 14, VIX-history-unavailable, chain fetch, no eligible strike/combo) are unaffected — always hard-abort regardless of `log_only_gates`. `main()`'s existing generic `record_gate_violation`/Telegram-annotation persistence path (already built for PP) now fires identically for CC/Collar. See DECISIONS.md 2026-08-07.
   - `portfolio/`: live portfolio P&L crons/snapshots (e.g. `daily_snapshot.py` EOD portfolio snapshot cron, `morning_nav.py` pre-market NAV fetch, `paper_snapshot.py`, `roll_leg.py`, `backup_db.py` online DB backup cron).
   - `intraday/`: intraday monitoring crons (e.g. `intraday_tracker.py` combined Dhan+Nuvama orchestrator, `nuvama_intraday_tracker.py`, `dhan_intraday_tracker.py`).
   - `reporting/`: `paper_pnl_report.py` (SNAP-4, 2026-08-07) — `build_pnl_report(store, strategy_name, as_of=None) -> PnLReport` (pure function, importable by a future graphing layer): daily P&L graph data (`DailyPnLPoint` per `paper_nav_snapshots` row, `total_pnl` recomputed as `realized_pnl + unrealized_pnl` at query time — never reads the stored `total_pnl` column, per SNAP-1's finding that 42/267 `paper_nav_snapshots` rows violate that invariant), `realized_since_inception` (via `get_strategy_realized_pnl()` from `src/paper/tracker.py`, summed from the append-only `paper_trades` ledger rather than the latest snapshot's `realized_pnl` — survives a strategy's full open→close→reopen cycle reset, confirmed live for `paper_nifty_futures` on 2026-08-05), `realized_this_month` (nav-snapshot baseline diff — latest row minus the last row strictly before the current month started, falling back to the latest row directly when the strategy opened mid-month; does not yet handle a cycle reset occurring *within* the current month — flagged as a known first-pass limitation, not silently ignored), `unrealized_since_inception` (latest snapshot's `unrealized_pnl`). `has_data=False` with all-zero aggregates when a strategy has zero `paper_nav_snapshots` rows (every IC variant until enough daily rows accrue). CLI: `--strategy <name>` (required), `--json` (plain text otherwise). 2 tests in `tests/unit/reporting/test_paper_pnl_report.py`. See `docs/archive/plan/paper-ic-daily-snapshot/stories.md` SNAP-4.
   - `seed/`: one-time DB seed scripts (`seed_mf_holdings.py`, `seed_nuvama_positions.py`, `seed_portfolio.py`, `seed_trades.py`).
   - `council/`: council workflow tooling (`ask_council.py`, templates).
   - `daemon/`: monitor daemon scripts (`monitor_daemon.py` main loop, `start_monitor.py` launcher, `stop_monitor.py` shutdown, `pre_market_brief.py` pre-market summary cron, `eod_summary.py` EOD P&L summary cron).
   - `dev/`: diagnostics/migrations (`send_test_telegram.py`, `validate_strategy_spec.py`, `probe_nuvama_schema.py`, `migrate_strike_to_text.py`, `test_api_version.py`, `paper_track_snapshot.py`).
`src/notifications/formatting.py` — Value formatters (money, greek, strike, pct) + table builders for Telegram MarkdownV2 notifications (formatting-rules epic, FMT-1..FMT-4, `docs/plan/telegram-markdown-migration/formatting-rules/`, closed 2026-08-25). `format_money` forces Decimal, `format_greek` handles None as `-`, `format_strike` reuses existing convention without decimals, `format_pct` prints whole numbers bare. `build_kv_table`/`build_side_by_side_kv_table`/`build_leg_table` (+ `LegRow` frozen dataclass) render bordered/fenced tables with widths always computed from actual content (`max(len(...))`), never a hand-counted constant — the bug class that broke `build_comparison_report()`'s original fixed-width budget. All formatters return unescaped text; callers still apply `mdcode()`/`escape_markdown()` before sending. Canonical per-parameter-type rules: root `FORMATTING.md`; module usage doc: `src/notifications/CLAUDE.md` §"Value Formatting & Table Builders".
`src/notifications/telegram_gateway.py` — `TelegramGateway`: council-free approval request dispatch + inbound callback polling + auth guard (chat-ID allowlist) + timeout scan for stale pending approvals. `send_approval_request(event, context_str)` builds inline keyboard from `event.payload["valid_actions"]` (list of action_type strings embedded by each strategy); returns `None` + logs ERROR if `valid_actions` missing. No `CouncilOutput` or LLM call in the approval path. Non-fatal contract: errors logged and suppressed, never raise to caller. `send_approval_request` and `send_notification` both send `parse_mode: MarkdownV2` (migrated from HTML, MD-4.1, telegram-markdown-migration epic, 2026-08-25); both already escape dynamic values via `escape_markdown()` at their call sites. `send_notification`'s 5 live callers (3 reporting builders + 2 IC entry-signal scripts) were escaped in the same sitting as the gateway's parse_mode flip (MD-4.2) to avoid reopening MD-2's live-risk window for entry notifications.
`src/instruments/lookup.py` — `get_expiry_candidates(underlying, today, preference)`: enumerates NIFTY expiries from BOD JSON into monthly (floor-only, DTE>=14, no ceiling — nearest live last-of-calendar-month date, resolved independently of the sequential per-date loop the same way `yearly` is; ceiling removed 2026-08-12 after the fixed 14–45 window proved narrower than the real 28/35-day gap between consecutive last-Tuesday-of-month dates, causing a recurring multi-day dead zone whenever a month has a 5th Tuesday — see DECISIONS.md 2026-08-12 "monthly DTE ceiling removed"; floor itself unchanged from the 2026-08-11 15→14 fix) / quarterly (DTE 46–200, last of Mar/Jun/Sep/Dec, unchanged — its 154-day window was never at risk of the same gap) buckets; weekly's Tuesday-claim (DTE<=14) excludes the single `dte==14 and is_monthly` overlap point so the two bands don't collide now that Nifty's monthly expiry itself falls on a Tuesday post-April-2026, but still claims any other last-of-month Tuesday at lower DTE (e.g. DTE 5) exactly as before; `yearly` is NOT a DTE band — it is always the nearest live last-of-December expiry, no lower DTE bound; rollover to next December happens for free once the current one settles and drops out of the live instrument feed (no floor/rollover logic needed). Deliberately independent of `quarterly` — the same December date satisfies both labels at once once it's inside quarterly's 46–200 band (fixed 2026-07-22, see DECISIONS.md BUG-015 — old DTE-band `is_yearly` (201–420) let `quarterly` claim any Dec inside 46–200 first, starving `yearly` and forcing a bogus rollover a full year out; a same-day follow-up correction removed an over-engineered `yearly_dte_floor` that reintroduced the same class of bug by rolling forward before the live December contract had actually settled). default preference `["monthly","quarterly","yearly"]`; custom order accepted for hedge use. `get_next_contract(instrument_key)`: given a futures or options instrument key, resolves the next-maturity contract from the BOD JSON by expiry order; returns `dict | None`; used by base-expiry roll alert for `base_futures` (safe — NIFTY futures are monthly-only). `get_next_contract_in_band(instrument_key, today, preference=None)`: band-aware variant for options — resolves the current strike, then uses `get_expiry_candidates()` to project it into the next monthly/quarterly/yearly expiry, skipping intervening weeklies; returns `dict | None`; used by base-expiry roll alert for `base_ditm_call` (2026-07-20 fix — see DECISIONS.md). `format_option_label(underlying, strike, option_type, expiry) -> str` / `format_leg_label(instrument_key, lookup) -> str` (telegram-leg-labels TL-1, 2026-08-07) — human-readable option labels (e.g. `NIFTY 22000 CE 07 JUL 26`) for Telegram/log prose; `format_leg_label` resolves via `InstrumentLookup.get_by_key`, falling back to the raw `instrument_key` (logged WARNING) when unresolved or non-option. Wired into the four overlay close-notification builders (`auto_close.py`, `cc_overlay_v1.py`, `collar_overlay_v1.py`, `pp_overlay_v1.py`, TL-2) and the IC entry preview message in `scripts/strategies/ic/paper_ic_entry.py` (TL-3, `cmds` command block untouched). Standard documented in `src/notifications/CLAUDE.md` §"Instrument Label Formatting" (TL-4).
`src/instruments/strike_selector.py` — `filter_strikes_by_delta()`, `rank_strikes()`, and `_apply_liquidity_gate()`: core functions for selecting Nifty option strikes from live chains based on delta bounds and liquidity gates.


`src/client/protocol.py` — `BrokerClient.get_order_margin(instruments)` (2026-07-22): pre-trade margin-calculator call, distinct from `get_margins` (account-level funds snapshot). Implemented in `UpstoxLiveClient` (real `POST /v2/charges/margin`, reads `UPSTOX_ACCESS_TOKEN` from settings directly — a scoped exception to that class's "Daily OAuth not wired" pattern, see DECISIONS.md) and `MockBrokerClient` (deterministic fake with a netting-benefit factor for BUY+SELL baskets). `PaperStore` gained `paper_margin_snapshots` table + `record_margin_snapshot`/`get_margin_snapshot`, keyed by `(strategy_name, entry_date)`; `MarginSnapshot` frozen dataclass in `src/paper/models.py`. `scripts/strategies/ic/ic_entry_gates.py::capture_entry_margin()` — shared, non-fatal helper called from both IC entry scripts after legs are confirmed persisted. `paper_ic_snapshot.py`'s EOD report gained a "ROI on margin" line. IC-only for now — CSP/CC/PP/Collar entry scripts not wired.

`src/config.py` — `Settings(BaseSettings)` singleton (pydantic-settings). Declares every env var used across `src/` and `scripts/`: Upstox tokens (`upstox_env`, `upstox_analytics_token`, `upstox_access_token`, `upstox_sandbox_token`, `upstox_debug`), Telegram (`telegram_bot_token`, `telegram_chat_id`), Nuvama (`nuvama_settings_file`), Dhan (`dhan_client_id`, `dhan_access_token`), data paths (`vix_data_dir`). Loads from `.env` + environment. Import the `settings` singleton — never call `os.getenv()` directly. **BUG-026, 2026-08-07:** `vix_data_dir` retyped `str` → `Path` (pydantic coerces a string env value automatically) — three `--auto-cc`/`--auto-collar`/`--auto-pp` bootstrap call sites in `paper_3track_overlay_entry.py` passed `settings.vix_data_dir` straight into `load_vix_series()`'s `.glob()` call with no `Path(...)` wrap (unlike every other caller, which already wrapped it defensively), so every cron invocation of those three overlay-entry paths crashed inside a bare `except Exception` at the IVR gate — silently, no Telegram alert, zero overlay trades ever landed despite the CC1–CC5/PP1–PP5/Collar1–Collar3b `--no-dry-run` unblock having shipped in 2026-08-02/03. 5 unit tests in `tests/unit/test_config.py` (was 3; +Path-type assertion, +env-override coercion test); 3 new regression tests in `tests/unit/paper/test_overlay_entry.py` exercise the real `load_vix_series()` call (not mocked) against a fixture Parquet dir for all three bootstrap functions, closing the coverage gap that let this ship (every pre-existing test for these functions mocked `load_vix_series` directly). **BUG, 2026-08-10:** all three `OverlayConfig` builders here hardcoded a stale `lot_size=75` (Nifty's current lot size is 65) — replaced with `_resolve_lot_size(lookup, instrument_key)`, which reads `lot_size` off the selected strike's own BOD record (`InstrumentLookup.get_by_key`), falling back to a corrected `_NIFTY_LOT_SIZE_FALLBACK = 75`→65 constant only if unresolvable. `src/strategy/nifty_track_comparison_v1.py`'s independent `_NIFTY_LOT_SIZE` constant (roll-target `LegSpec.quantity`) had the same staleness, fixed the same way via a new `_resolve_lot_size` instance method — its synthetic roll-target instrument keys don't match real BOD records, so that path still relies on the fallback constant in production (deferred: switch to `InstrumentLookup.search_options`). See DECISIONS.md 2026-08-10.
`src/utils/logging.py` — `setup_logging(*, json: bool | None = None, level: str | None = None)`: configures structlog with shared processors (contextvars merge, log level, logger name, ISO timestamp). JSON renderer in prod (`upstox_env == "prod"`); `ConsoleRenderer` otherwise. Wired at entry point of every script in `scripts/`. 2 unit tests in `tests/unit/utils/test_logging.py`.
`scripts/healthcheck.py` — dead man's switch for EOD cron validation. Seven checks in order: trading-day guard (silent exit on holidays), DB accessibility, `daily_snapshots` recency, `paper_nav_snapshots` recency, VIX data recency (warn if > 2 days stale), disk space (warn if < 500 MB free), and `paper_3track_snapshot` cron crash detection (BUG-029/B029.5 — parses `logs/paper_snapshot.log` for an unhandled Traceback between today's `[paper_3track_snapshot]`-tagged lines and the next cron entry's `[paper_snapshot]`-tagged lines; `--cron-log-path` overrides the default `logs/paper_snapshot.log`). Silent on full pass (exit 0); fires Telegram alert and exits 1 on any failure or warning. Intended cron: `30 16 * * 1-5`. 7 unit tests in `tests/unit/test_healthcheck.py`.

**Developer Tooling (repo root):**
`pyproject.toml` — project metadata, all dev dependencies (`pytest`, `ruff`, `mypy`, `pre-commit`, `vulture`, `bandit`, `commitizen`), `[tool.pytest.ini_options]` (testpaths, asyncio_mode, markers, `addopts = "-n auto"` for parallel), `[tool.ruff]` lint/format rules, `[tool.mypy]` with phased strict rollout (strict on `src/client.*` + `src/paper.*`; permissive elsewhere; third-party stubs silenced), `[tool.coverage.run]` (source=src) + `[tool.coverage.report]` (fail_under=80, excludes pragma/TYPE_CHECKING/NotImplementedError/@abstractmethod). Install: `pip install -e ".[dev]"`.
`Makefile` — targets: `test` (parallel, `-n auto`), `test-serial` (no randomly, for debugging), `coverage`, `lint`, `fmt`, `security`, `dupes`, `dead-code`, `ci` (lint → test → coverage → security, `--randomly-seed=last`), `index` (2026-07-24: was referenced by `scripts/dev/post_commit_hook.sh` and this doc for years with no matching target — added `codebase-memory-mcp cli index_repository '{"repo_path": "$(CURDIR)"}'`, per the tool's real CLI reference; unverified against an actual `codebase-memory-mcp` binary since none is installed in the Cowork sandbox — confirm on your host before relying on it). `make ci` is the single entry point for all dev and CI checks.
`.pre-commit-config.yaml` — three hooks: `ruff` (lint + fix), `ruff-format`, `mypy` (scoped to `src/client/` + `src/paper/` only), `detect-secrets`. Install: `bash scripts/dev/install_hooks.sh`.
`scripts/dev/install_hooks.sh` — installs pre-commit + post-commit hook in one command.
`scripts/dev/post_commit_hook.sh` — post-commit: echoes re-index reminder when `src/` or `scripts/` changed.
`docs/plan/dev-foundation/dx-foundation/mypy_baseline.md` — mypy error counts per module at DX-3 baseline. Use to track type-safety progress over time.
`.github/workflows/ci.yml` — GitHub Actions CI workflow. Triggers on push/PR to `main`. Matrix: Python 3.10 + 3.11. Steps: checkout → setup-python (pip cache) → `pip install -e ".[dev]"` → `make ci` (UPSTOX_ENV=test, forces MockBrokerClient) → coverage XML + `irongut/CodeCoverageSummary` summary action → `actions/upload-artifact` (htmlcov, 7-day retention, Python 3.10 only). No secrets required — `UPSTOX_ENV=test` bypasses all live API calls. `@pytest.mark.sandbox` tests excluded by default. Coverage thresholds: warn at 70%, fail at 80%.

**Research tooling (docs/):**
`docs/strategies/regime_probe.pine` — Pine Script v6 sensor script (not a trading strategy). Outputs a structured 22-row `table.new()` on the last bar covering: regime label, regime code (−2→3), options recommendation, full ADX/DI block, ATR with 252-bar percentile rank, BB width with percentile rank, RSI, 20-day annualized HV, India VIX via `request.security()`, and bar date. Designed for MCP extraction via `data_get_pine_tables`. Study identified by name (stable across sessions). Key-value fallback via `label.new()` pipe-delimited text. Operational tool: load on NIFTY 1D and 1W at each paper trade entry (BACKTEST_PLAN.md Phase 0 gate criteria C/D).
`docs/archive/tv_mcp_testing_framework.md` — 7-phase capability probe for `tradesdontlie/tradingview-mcp` (CDP-based, connects to TradingView Desktop). Validated 2026-05-23: Phase 3 confirmed table readability end-to-end; Phase 3C confirmed timeframe switching updates correctly (no stale data). Archived — findings fully captured in `DECISIONS.md → TradingView MCP Regime Probe`.


---

## CONTEXT.md "Test Coverage" (verbatim snapshot, 2026-08-27)

> Trimmed in `CONTEXT.md` to a count + last-green date per RDO-1; the per-module breakdown
> below was a stale historical snapshot ("not re-verified this pass"). Kept here verbatim.

## Test Coverage

- **Total: ~2982 tests** (2980 passed, 2 skipped — live `pytest tests/unit/` run, 2026-08-26 md-cleanup reorg). Per-module breakdown below is a historical snapshot, not re-verified this pass. Earlier snapshot included CH-9b hypothesis property tests: 19 @given tests across `test_ivr_hypothesis.py`, `test_delta_hypothesis.py`, `test_pnl_hypothesis.py`) (paper module: 92 tests across Phases A–D all passing; 6 new expiry-candidate tests in `tests/unit/instruments/test_expiry_candidates.py`; strategy module exit-signals additions: `test_exit_signals.py` 20 tests, `test_cc_overlay_v1.py` 11, `test_pp_overlay_v1.py` 10, `test_collar_overlay_v1.py` 11, `test_overlay_closer.py` 10, `test_csp_nifty_v1.py` 29; pre-existing failures in `test_upstox_live.py` + `test_mock_client.py` — `pytest-asyncio` not installed in sandbox, not code regressions)
- Run: `python -m pytest tests/unit/`
- Auth tests: `tests/unit/auth/` (64 tests — Nuvama login + verify, Dhan login + verify)
- MF tests: `tests/unit/mf/` (127 tests)
- Portfolio tests: `tests/unit/portfolio/` + `tests/unit/test_portfolio.py` (94+ tests — includes 4 record_roll store tests + 10 _build_trades script tests)
- Client tests: `tests/unit/test_client.py`, `test_protocol.py`, `test_exceptions.py`, `test_factory.py`, `test_mock_client.py`, `test_upstox_live.py` (90+ tests)
- Snapshot tests: `tests/unit/test_daily_snapshot_historical.py`, `test_daily_snapshot_helpers.py`, `test_notifications.py` (50+ tests)
- Dhan tests: `tests/unit/dhan/` (152 tests — models, positions parser/filter/formatter, store options+margin, daily_snapshot integration)
- Nuvama tests: `tests/unit/nuvama/` (169 tests — bond models, bond store, reader, seed, **NuvamaOptionPosition + NuvamaOptionsSummary models (AR-3), parse_options_positions + build_options_summary (AR-3), record_all_options_snapshots atomic (AR-7), record_intraday_positions, get_intraday_extremes, purge_old_intraday (AR-3), get_monthly_realized_pnl (Phase E)**; test_portfolio_summary_nuvama.py deleted — superseded by composed model structure)
