<!-- Copy with the `epic/` folder. Delete these HTML comments once filled in. -->

# <Epic title> — epic index

> One-paragraph statement of what the epic delivers and why it is one epic rather than
> separate stories.

## Why this epic exists

<!-- The problem, the trigger, the origin (session / review / council). Prose lines filled to ≤200 chars. -->

## Scope decisions

<!-- The load-bearing choices already made, and with whom ("confirmed with Animesh,
YYYY-MM-DD"). These are the decisions a sub-story must not silently re-open. -->

## Stories

| Story | Purpose | Status | Depends on | Closing SHA |
|---|---|---|---|---|
| `<story-a>/` | <one line> | ⬜ Not started | — | — |
| `<story-b>/` | <one line> | ⬜ Not started | `<story-a>` | — |
| `<story-c>/` | <one line> | ⬜ Not started | `<story-b>` | — |

Status: ⬜ Not started · 🔄 In progress · ✅ Done. This column is the epic's progress view —
per-task checkboxes live only in each sub-story's `tasks.md`.

## Cross-cutting constraints

<!-- Rules every sub-story must honour — non-fatal contracts, Decimal/TEXT invariants,
"every X migrates not just Y", naming conventions. A constraint that applies to only one
story belongs in that story's files, not here. -->

## Supersession / coordination

<!-- Other epics or stories this one supersedes or must coordinate with, and the current
state of that coordination. -->

## Epic done when

<!-- Prose acceptance list, no `- [ ]` checkboxes (§Conventions "Checkbox consistency").
One bold story id per line + its done criterion. Verified at epic close. -->

- **<story-a>** — <criterion>
- **<story-b>** — <criterion>
- **<story-c>** — <criterion>
